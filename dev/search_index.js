var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#AIRMED-API-Contracts","page":"API contracts","text":"","title":"AIRMED API Contracts"},{"category":"section","location":"api/#Core-Types","page":"API contracts","text":"","title":"Core Types"},{"category":"section","location":"api/#AIRMEDProblem","page":"API contracts","text":"Constructor (keyword arguments):\n\nField Type Required Description\nname String yes Problem identifier\nmodel ODESystem yes MTK equation-based model, unsimplified\ncomponent_hooks Vector{ComponentHook} no Topological positions at which an unknown component may be inserted\ncomponent_guesses Vector{ComponentGuess} no Candidate component types, each with a factory; see below\nadaptable_params Vector{AdaptableParam} no Existing model parameters that propose_model_adaptation may re-fit directly, tried before the structural search; see below\noptimizable_params Vector{Symbol} no Parameter names eligible for continuous calibration\ndata_source DataSource yes Measurement data source\ndrift_config DriftConfig no Drift detection configuration; has defaults\nsupervision_config SupervisionConfig no Agent supervision configuration; has defaults\ntspan Tuple{Float64, Float64} yes Simulation time span (t_start, t_end)\nu0 MTK state map or Vector yes Initial conditions\np0 MTK parameter map or Vector yes Parameter values\nmodel_preamble Vector{String} no using and import lines written into the generated adaptation code. Must include every domain library required to instantiate the proposed components, e.g. \"using ModelingToolkitStandardLibrary.Electrical\". AIRMED imports no domain libraries itself.\nobservable_states Vector of MTK symbolic vars no State variables for which physical sensors exist. The rows of data_states correspond to these variables in order. Only these are compared against measurement data during parameter fitting of the adapted model, so internal states of newly added components, which have no sensors, are excluded. If empty, the first n_data rows of Array(sol) are used. Example: [cap.v].\nport_aliases Dict{String,String} no Normalises port names emitted by an LLM to those of the caller's library, e.g. \"+\" => \"p\". Empty by default, since AIRMED assumes no naming convention.\ncomponent_library_label String no Label of the component library shown in prompts, e.g. \"ModelingToolkitStandardLibrary.Electrical\".\nadaptation_guesses Dict{Any,Any} no Numeric initial guesses for variables of existing components whose initialisation becomes ambiguous once a new branch is added. Complements ComponentGuess.connector_guesses, which covers the connectors of the new component.\nhook_residuals Vector{HookResidualSpec} no Per-hook balance laws enabling the hook-local measurement. Empty by default, in which case the hook-local step is skipped.\nobservation_noise Vector{Float64} no 1 sigma sensor noise per row of data_states, in the units of that row. Required together with hook_residuals, since without a noise floor no signal-to-noise statement is possible. Its length must equal that of observable_states.\n\nThe constructor automatically calls structural_simplify(model) and validates that every HookResidualSpec names a declared hook and that observation_noise matches observable_states in length.\n\n","title":"AIRMEDProblem"},{"category":"section","location":"api/#ComponentGuess","page":"API contracts","text":"Describes a candidate component type that could explain an observed deviation. The factory encapsulates all domain-specific library knowledge; the core of AIRMED imports no domain package.\n\nComponentGuess(\n    component_type :: Symbol,          # e.g. :Resistor\n    description    :: String,\n    factory        :: Function;        # (name::Symbol, param_val::Float64) -> ODESystem\n    parameter_name    :: Symbol = :R,          # the fitted parameter's name on the component\n    default_parameter :: Real   = 1.0,         # starting point when the LLM names no value\n    param_min         :: Real   = 1e-12,       # fitting runs in log-space: both must be > 0\n    param_max         :: Real   = 1e12,\n    connector_names   :: Tuple{Symbol,Symbol} = (:p, :n),   # which ports get wired\n    connector_guesses :: Dict   = Dict(),      # initial values for the new connectors\n)\n\nFactory contract: receives the desired component name and the initial parameter value; returns a fully constructed, named ODESystem.  Example:\n\nComponentGuess(:Resistor, \"Series resistor\",\n    (name, val) -> Resistor(; R = val, name = name);\n    parameter_name = :R, default_parameter = 500.0,\n    param_min = 10.0, param_max = 1e6,\n    connector_names = (:p, :n),\n    connector_guesses = Dict(:v => 230.0, :i => 0.5))\n\nconnector_guesses is required when the connector variables of a new component appear in the default expression of another component. The MTK initialisation DAE then contains cyclic symbolic substitutions, which a few numeric values resolve.\n\n","title":"ComponentGuess"},{"category":"section","location":"api/#ComponentHook","page":"API contracts","text":"Marks a topological position where an unknown component might be inserted.\n\nComponentHook(\n    name        :: Symbol,                     # e.g. :r1_parallel\n    port_a      :: Tuple{Symbol, Symbol},      # (subsystem, port), maps to .p\n    port_b      :: Tuple{Symbol, Symbol},      # (subsystem, port), maps to .n\n    description :: String,\n)\n\nThe same subsystem in both ports denotes a parallel insertion, different subsystems a series insertion, in which the existing direct connection is replaced.\n\n","title":"ComponentHook"},{"category":"section","location":"api/#HookResidualSpec","page":"API contracts","text":"Declares the conservation law that holds at a hook. If the law does not close, the imbalance is the through- or across-variable of the missing element, obtained directly from the sensors without simulation.\n\nHookResidualSpec(\n    hook           :: Symbol,                      # must name a declared ComponentHook\n    kind           :: Symbol,                      # :parallel or :series\n    drive,                                         # an entry of observable_states\n    response_terms :: Vector{Pair{Any,Float64}},   # observable => signed coefficient\n    description    :: String = \"\",\n)\n\nWith :parallel, the response is a flow variable from a node balance, driven by the measured potential across the hook. With :series, the response is a potential from a loop balance, driven by the measured flow through it.\n\nDomain-independent. Nothing in this specification is electrical; the same structure applies to force and velocity, torque and angular velocity, mass flow and pressure, and heat flow and temperature. AIRMED sums signed channels and propagates variances without naming a physical quantity.\n\nSupplied by the caller, as is ComponentGuess.factory. Deriving it automatically would require access to the connection-set internals of MTK and would tie the core to one generation of that library.\n\nHookResidualSpec(:fridge_parallel, :parallel, load_fridge.v,\n                 [r_grid.i => 1.0, pv_conv.i => 1.0, bat_conv.i => 1.0,\n                  load_fridge.i => -1.0, load_const.i => -1.0],\n                 \"extra path parallel to the consumers\")\n\nRequires observable_states and observation_noise to be non-empty; the hook-local step is skipped otherwise.\n\n","title":"HookResidualSpec"},{"category":"section","location":"api/#AIRMED.hook_snrs(problem,-data_times,-data_states;-smooth_w-15)-Dict{Symbol,Float64}","page":"API contracts","text":"Not exported; call it as AIRMED.hook_snrs. Most callers instead set restrict_hooks_by_snr = true on propose_model_adaptation, which applies it internally.\n\nBalance-law signal-to-noise at every declared hook. For each HookResidualSpec: sum the response terms with their signs, propagate the independent sensor variances in quadrature, smooth the summed residual, and divide by the noise floor scaled to match:\n\nSNR = RMS(smooth(Σ cᵢ·xᵢ, w)) / (√(Σ (cᵢ·σᵢ)²) / √w)\n\nThe factor √w is required: the numerator is smoothed, so comparing it against the raw per-sample sigma understates the noise floor by that factor and marks quiet hooks as active.\n\nAn SNR near 1 means that the law closes to within measurement noise, so nothing is missing at that hook and no further analysis of these data can establish otherwise. The function is public so that a caller can restrict the prompt to the positions supported by the measurements.\n\n","title":"AIRMED.hook_snrs(problem, data_times, data_states; smooth_w = 15) -> Dict{Symbol,Float64}"},{"category":"section","location":"api/#AdaptableParam","page":"API contracts","text":"An existing model parameter that propose_model_adaptation may re-fit directly. This is the simplest adaptation outcome, with no new component and no new equations. A drift caused by a change of an existing component's parameter is then diagnosed as such rather than by inserting a component next to it.\n\nAdaptableParam(\n    name          :: Symbol,   # e.g. :r_load_R\n    param,                     # the symbolic parameter, e.g. r_load.R\n    default_value :: Real,     # nominal value, the starting point of the fit\n    description   :: String = \"\";\n    param_min :: Real = 1e-12, # bounds; fitting runs in log-space, both positive\n    param_max :: Real = 1e12,\n)\n\npropose_model_adaptation tries every declared AdaptableParam individually, since varying several jointly risks non-identifiability, and does so before building a prompt or searching component_hooks. The pass makes no LLM call and consists of local optimisation only. If one parameter explains the drift, i.e. no residual drift after fitting and no value at a bound, it is accepted immediately: ModelAdaptationResult.parameter_adjustments is populated and proposals remains empty. Only if none does, the structural search over component_hooks and component_guesses follows.\n\n","title":"AdaptableParam"},{"category":"section","location":"api/#Model-Adaptation","page":"API contracts","text":"","title":"Model Adaptation"},{"category":"section","location":"api/#propose_model_adaptation(problem,-update_result;-kwargs...)-ModelAdaptationResult","page":"API contracts","text":"Diagnose a drift either as a re-fitted existing parameter, which is preferred, see AdaptableParam above, or as concrete MTK component proposals.\n\nupdate_result may be nothing; a UDE is not required. If none is present, or with include_ude_sections = false, every section describing it is omitted from the prompt and the diagnosis rests on the sensor residuals and the hook-local step.\n\nOrder of operations. The existing-parameter pre-pass runs before any characterisation is computed. If it resolves the drift, the function returns immediately and a characterisation computed earlier would be discarded.\n\nKey keyword arguments:\n\nArgument Default Description\ndata_times nothing Measurement time vector, required for parameter fitting\ndata_states nothing Measurement state matrix (n_states × n_times)\napi :none LLM backend; see Backends below\napi_key \"\" API key (ignored for :none and local servers)\nbase_url \"\" Endpoint override; also carries the CLI path for :claude_cli\nmodel \"\" Model name passed through to the backend\nbuild_model false If true, attempt to build the generated ODESystem\nfit_params true Fit primary parameter of each proposal to data\nfit_iters 300 Max LBFGS iterations for parameter fitting\nfit_lr 5e-2 Learning rate (fitting runs in log-space)\noptimization_algorithm OptimizationOptimJL.LBFGS() Optimiser used for parameter fitting\nmax_retries 3 Additional LLM attempts after a failed validation; see Retry and escalation\nholdout_fraction 0.25 Trailing fraction withheld from fitting, used for validation\nn_samples 50 Grid samples per input when a UDE correction is characterised (UDE path only)\nhook_local_analysis true false keeps the balance-law residual and its SNR but omits the characterisation\nhook_summary nothing Pre-computed hook-local section, used verbatim. \"\" omits the section entirely; nothing recomputes it\nrestrict_hooks_by_snr false Offer only hooks whose measured SNR clears hook_snr_min\nhook_snr_min 3.0 Threshold for the above\ninclude_ude_sections true Forced false when update_result === nothing\n\nPrompt content. Each of these changes what the model is told and therefore what an evaluation measures. Both default to false; results are comparable only between runs that used the same settings.\n\nArgument Default Description\ninclude_component_equations false List each offered component type with its constitutive equations rather than its type name alone\nhook_terminal_inputs false Fit the hook residual in the terminal quantities of the element itself, i.e. its drive, the derivative and integral of that drive, and time, instead of every measured channel. If the topology contains a slack element, the channel of another component can otherwise restate conservation and outscore the true constitutive law\n\nLLM transport:\n\nArgument Default Description\nllm_timeout 180 HTTP read timeout in seconds per call. Local CPU backends require considerably more on long prompts; the evaluation scripts use 600\nllm_log_file nothing Append every prompt and response pair to this file\nnum_ctx nothing :ollama_native only; nothing falls back to AIRMED_OLLAMA_NUM_CTX\nthink nothing :ollama_native only; nothing falls back to AIRMED_OLLAMA_THINK\n\nThree levels of hook-local evidence are reachable through these arguments, and they are not equivalent:\n\nLevel Setting Prompt carries\nfull hook_local_analysis = true balance law + SNR + verdict + characterisation\nlocalisation only hook_local_analysis = false balance law + SNR + verdict\nnone hook_summary = \"\" no hook section at all\n\nThe middle level still provides the localisation: the per-hook verdict, stating that a residual is present or that nothing is missing at that hook, restates the SNR in words. An ablation that treats this level as a no-evidence baseline measures only what the characterisation adds beyond the localisation.\n\nWhen api = :none (demo mode), each ComponentHook is paired with the first ComponentGuess; parameter values are fitted to data when data_times / data_states are provided.","title":"propose_model_adaptation(problem, update_result; kwargs...) -> ModelAdaptationResult"},{"category":"section","location":"api/#Retry-and-escalation","page":"API contracts","text":"Each attempt consists of building the prompt, calling the LLM, parsing and winnowing the response, fitting the parameters of the proposal, re-simulating and re-checking drift. The pipeline then judges structural adequacy per observable channel against the declared sensor noise, not against a UDE loss and not averaged across channels:\n\nstructural_doubt = drift_detected || any(rmse_channel > accept_factor(n, w) × σ_channel)\n\naccept_factor is a one-sided chi-quantile correction for the finite sample size (_hl_accept_factor, about 1.12 at n = 200). A retry is triggered when structural_doubt holds or a fitted parameter lies at a ComponentGuess plausibility bound, provided the budget max_retries is not exhausted. The re-asked prompt is the same prompt plus a list of the attempts already made, each with its proposals, fit loss, drift verdict, per-channel RMSE and bound flags. No simulation output and no optimiser trace is added.\n\nOnce the budget is exhausted, the best attempt is returned, ranked by fit_loss * (1 + complexity_penalty * n_components).\n\nThe retry threshold is stricter than the escalation threshold, about 1.12 sigma against 5 sigma. A correct proposal whose fit lies slightly above the noise floor is therefore re-asked although it would not escalate. The additional call costs time but cannot degrade the returned answer, since the best attempt is still selected.\n\nArgument Default Effect\nescalate_on_inadequate_structure false Once the budget is exhausted, reject the best proposal if its worst channel exceeds 5 times the sensor noise, or if fitting produced no loss at all, i.e. the model did not build or no fittable parameter was found, in which case fit_loss = Inf and every proposal carries its unfitted default. Then success = false, message reports the rejection, and escalation contains an account produced by a separate LLM call. Disabled by default, since it changes success for callers whose retries end on a poor verdict\nexplain_on_failure false If the LLM returns nothing usable, skip the demo-mode enumeration, which would pair hooks with library components without evidence while still reporting success, and fill escalation with an explanation instead\n\nThe structure-quality verdict in message uses the same scale: good at or below 1.5 sigma on every channel, partial at or below 5 sigma, and poor above that.\n\nModelAdaptationResult fields:\n\nField Type Description\nproposals Vector{ComponentProposal} Insertion recommendations; empty if the fix consisted of parameter adjustments only\nparameter_adjustments Vector{ParameterAdjustment} Existing parameters re-fitted in place, without a structural change; empty if a ComponentProposal was used\ngenerated_code String Julia/MTK source for the adapted system; a comment-only snippet for a parameter-only fix\nadapted_model Union{ODESystem, Nothing} The built ODESystem, only with build_model = true\nprompt String The prompt that was sent, or would have been sent\nraw_response String The raw response, or a stub message\napi Symbol The backend that was used\nsuccess Bool Whether at least one proposal or parameter adjustment was produced and, with escalate_on_inadequate_structure = true, was not rejected as inadequate\nmessage String Status summary, including the structure-quality verdict\nfit_loss Float64 MSE of the adapted trajectory against the measurement data after parameter fitting; NaN if fitting was skipped or failed\nconversation Vector{LLMExchange} All exchanges in order, including retried and discarded attempts. The note of the accepted attempt is prefixed accordingly. Empty if no LLM call was made, e.g. when the pre-pass resolved the drift\nescalation String Account of why no adaptation could be made. Empty on ordinary paths; populated only through escalate_on_inadequate_structure or explain_on_failure\n\nprompt and raw_response refer to the accepted attempt; conversation contains the rejected ones.\n\nLLMExchange fields: attempt::Int, prompt::String, raw_response::String, n_parsed::Int, note::String.","title":"Retry and escalation"},{"category":"section","location":"api/#model_adaptation_summary(result)-String","page":"API contracts","text":"Multi-line human-readable summary of proposals and generated code.\n\n","title":"model_adaptation_summary(result) -> String"},{"category":"section","location":"api/#LLM-Backends","page":"API contracts","text":"Selected by the api keyword. base_url picks the actual server; api is mostly a routing label.\n\napi Transport Notes\n:none none Demo mode: pairs each ComponentHook with the first ComponentGuess. No network.\n:anthropic HTTPS Structured output via forced tool use, so no JSON repair is needed.\n:openai, :ollama, :groq, :openrouter HTTPS All speak the OpenAI chat/completions format; base_url selects the server. A local server needs no api_key.\n:claude_cli subprocess A local Claude Code CLI in print mode (claude -p). base_url carries the executable path.\n:ollama_native HTTPS The native /api/chat endpoint of Ollama. Required for reasoning models; see below.","title":"LLM Backends"},{"category":"section","location":"api/#When-to-use-:ollama_native","page":"API contracts","text":"The OpenAI-compatible /v1 shim accepts think and ignores it. A reasoning model then spends its completion budget on a reasoning field and returns empty content. :ollama_native posts to /api/chat with think = false and an explicit options.num_ctx.\n\nOn an 8B model running on CPU, a prompt of about 4k tokens exceeded a 600 s HTTP timeout with reasoning enabled and completed in 44 s with think = false. At a generation rate of about 3 tokens per second, 2000 reasoning tokens correspond to roughly 11 minutes.\n\n:ollama is kept separate and unchanged, so that results obtained with it remain comparable.","title":"When to use :ollama_native"},{"category":"section","location":"api/#Environment-variables","page":"API contracts","text":"Variable Default Effect\nAIRMED_LLM_MAX_TOKENS 2048 Completion budget. Increase it for reasoning models, since the reasoning draws on the same budget and a truncated answer is recorded as an unparseable response rather than a modelling error.\nAIRMED_OLLAMA_NUM_CTX 8192 options.num_ctx for :ollama_native. Ollama otherwise allocates the declared maximum context of the model, which can reserve several times the memory required by the weights.\nAIRMED_CLAUDE_CLI claude Path to the CLI for :claude_cli.\n\n","title":"Environment variables"},{"category":"section","location":"api/#Rebuilding-the-Adapted-Twin","page":"API contracts","text":"After propose_model_adaptation has determined what was missing, callers require the corrected model as a runnable AIRMEDProblem, for example to re-check drift on a fresh window or to compare base and adapted predictions against the measurement. The rebuild is mechanical, since everything it needs is available on the problem the adaptation ran against, so it is part of the framework rather than of each caller's script.","title":"Rebuilding the Adapted Twin"},{"category":"section","location":"api/#build_fix_problem(problem,-adapt_result;-kwargs...)-(prob-nothing,-msg,-comps-nothing)","page":"API contracts","text":"Dispatcher over the two forms a ModelAdaptationResult can take. If both are present, the parameter fix takes precedence, matching the ordering of the pipeline. comps is nothing for a parameter fix, since no component is created.","title":"build_fix_problem(problem, adapt_result; kwargs...) -> (prob | nothing, msg, comps | nothing)"},{"category":"section","location":"api/#build_adapted_problem(problem,-proposals;-kwargs...)-(prob-nothing,-msg,-comps-nothing)","page":"API contracts","text":"The structural fix as a runnable problem. comps holds the created component objects in proposal order, so that zip(proposals, comps) associates a component's current or power with the position it represents.","title":"build_adapted_problem(problem, proposals; kwargs...) -> (prob | nothing, msg, comps | nothing)"},{"category":"section","location":"api/#build_parameter_adjusted_problem(problem,-adjustments;-kwargs...)-(prob,-msg)","page":"API contracts","text":"The parameter-only fix: identical topology, adjusted values in p0. Adjustments naming a parameter absent from p0 are skipped and reported in msg.","title":"build_parameter_adjusted_problem(problem, adjustments; kwargs...) -> (prob, msg)"},{"category":"section","location":"api/#build_adapted_system(problem,-proposals;-init_vals-nothing)-(sys-nothing,-guesses,-msg,-comps-nothing)","page":"API contracts","text":"The rebuilt ODESystem, unsimplified. AIRMEDProblem applies structural_simplify itself, and the raw system retains the caller's subsystem objects, which permits symbolic indexing of the solution, e.g. sol[load.i]. init_vals defaults to the fitted parameter value of each proposal, falling back to the matching ComponentGuess.default_parameter.","title":"build_adapted_system(problem, proposals; init_vals = nothing) -> (sys | nothing, guesses, msg, comps | nothing)"},{"category":"section","location":"api/#rebuild_problem(problem;-kwargs...)-AIRMEDProblem","page":"API contracts","text":"Copy a problem, overriding only the named fields. All remaining fields are carried over, so replacing the model retains the solver window, the initial conditions and the drift settings.\n\nShared keyword arguments for the three build_*_problem functions:\n\nArgument Default Description\nname \"adapted twin\" Name for the rebuilt problem\ndata_source inherited Replay source for the rebuilt twin\nobservable_states inherited Narrow this to evaluate the twin on fewer channels than the adaptation used\n\nThe rebuilt twin is intended for simulation, not for a further adaptation: component_hooks, adaptable_params, hook_residuals and observation_noise are cleared. The last two are cleared together, since the constructor rejects a HookResidualSpec naming an undeclared hook.\n\nSeries insertions are handled: the existing direct connection between the two ports is located and replaced with two connections through the new component.\n\n","title":"rebuild_problem(problem; kwargs...) -> AIRMEDProblem"},{"category":"section","location":"api/#Simulation","page":"API contracts","text":"","title":"Simulation"},{"category":"section","location":"api/#simulate(problem;-saveat,-solver,-abstol,-reltol,-kwargs...)-ODESolution","page":"API contracts","text":"Run the base MTK model. Returns a SciML ODESolution.","title":"simulate(problem; saveat, solver, abstol, reltol, kwargs...) -> ODESolution"},{"category":"section","location":"api/#simulate_ude(ude!,-u0,-tspan,-p;-saveat,-solver,-...)-ODESolution","page":"API contracts","text":"Run a plain-Julia UDE ODE function. p must be a ComponentArray with field nn.","title":"simulate_ude(ude!, u0, tspan, p; saveat, solver, ...) -> ODESolution"},{"category":"section","location":"api/#load_data(source::DataSource,-tspan,-n_points)-(times,-states)","page":"API contracts","text":"Load or generate measurement data. states is (n_states × n_times).","title":"load_data(source::DataSource, tspan, n_points) -> (times, states)"},{"category":"section","location":"api/#interpolate_to_times(sol,-target_times)-Matrix","page":"API contracts","text":"Interpolate an ODE solution to arbitrary time points.\n\n","title":"interpolate_to_times(sol, target_times) -> Matrix"},{"category":"section","location":"api/#Validation","page":"API contracts","text":"","title":"Validation"},{"category":"section","location":"api/#compute_residuals(sim_times,-sim_states,-data_times,-data_states)-(times,-residuals)","page":"API contracts","text":"Linear interpolation of simulation to measurement times, then |sim - meas|.","title":"compute_residuals(sim_times, sim_states, data_times, data_states) -> (times, residuals)"},{"category":"section","location":"api/#detect_drift(residuals,-config::DriftConfig)-DriftResult","page":"API contracts","text":"Accepts AbstractVector (scalar) or AbstractMatrix (multi-state; averaged across states).\n\nDriftResult fields:\n\ndrift_detected::Bool\ndrift_index::Union{Int, Nothing}\nmean_residual::Float64\nmax_residual::Float64\nmethod::Symbol\ndetails::Dict{Symbol, Any}: algorithm-specific data, e.g. CUSUM or EWMA statistics\n\n","title":"detect_drift(residuals, config::DriftConfig) -> DriftResult"},{"category":"section","location":"api/#ML-Update","page":"API contracts","text":"","title":"ML Update"},{"category":"section","location":"api/#build_nn(input_dim,-hidden_dim,-output_dim;-depth2)-Lux.Chain","page":"API contracts","text":"Construct a fully-connected tanh network.","title":"build_nn(input_dim, hidden_dim, output_dim; depth=2) -> Lux.Chain"},{"category":"section","location":"api/#train_symbolic_ude(ude_prob,-sym_nn,-θ,-data_times,-data_states;-extra_params[],-solverTsit5(),-max_iters5000,-lr0.01,-verbosefalse)-UpdateResult","page":"API contracts","text":"The symbolic-UDE path: trains a UDE whose unknown term is a @SymbolicNeuralNetwork-embedded Lux network already present in ude_prob's equations. Uses AutoForwardDiff rather than Zygote. Populates the sym_nn / sym_theta / fitted_ode_prob fields of UpdateResult; evaluate the learned function with result.fitted_ode_prob.ps[result.sym_nn](x, result.fitted_ode_prob.ps[result.sym_theta]).","title":"train_symbolic_ude(ude_prob, sym_nn, θ, data_times, data_states; extra_params=[], solver=Tsit5(), max_iters=5000, lr=0.01, verbose=false) -> UpdateResult"},{"category":"section","location":"api/#train_ude(base_ode!,-nn,-nn_input_fn,-u0,-tspan,-data_times,-data_states,-p_base;-max_iters,-lr,-...)-UpdateResult","page":"API contracts","text":"Train the UDE. p_base is passed directly to base_ode! (not differentiated). nn_input_fn(u, t) -> Vector{Float32} selects NN inputs from state + time.\n\nAdditional keyword arguments:\n\nnormalize = true: wrap the network with input z-scoring and output rescaling derived from the data, which makes training robust across domains. The returned UpdateResult.nn is the wrapped network\npatience = 100: stop early when the best loss has not improved for this many iterations\nloss_target = 0.0: stop early when the loss falls below this value\npolish = true, polish_iters = 100: LBFGS polish after the Adam phase\n\nThe iterate with the lowest loss is returned, not the last one.\n\nmax_iters <= 0 selects the untrained control. No optimisation runs: both the Adam phase and the LBFGS polish are skipped, the network is returned as Lux.setup initialised it, final_loss is the loss at initialisation rather than Inf, and n_iterations is 0. A small positive max_iters is not an approximation of this, since the polish condition is final_loss > loss_target with loss_target = 0.0, so any positive value triggers a full 100-iteration LBFGS fit that dominates the result. With normalize = true the data-derived scaling still applies, so an untrained correction has a plausible magnitude and a random shape.\n\nn_iterations counts Adam callback invocations only. The polish solve runs without a callback, so a result reporting n_iterations = 300 may have received up to polish_iters further quasi-Newton steps.\n\nUpdateResult fields:\n\nsuccess::Bool\nfinal_loss::Float64\nn_iterations::Int\nnn: Lux network, including the normalisation wrappers with normalize=true\ntrained_nn_params: Lux parameter struct\ntrained_nn_state: Lux state struct\nmessage::String\nsym_nn, sym_theta, fitted_ode_prob: symbolic-UDE path only, otherwise nothing\ntrajectory_inputs: network inputs along the fitted trajectory, of size (n_inputs × n_times), or nothing\nmetrics::Dict{Symbol,Any}: :per_state_rmse_base, :per_state_rmse_ude, :drift_explained","title":"train_ude(base_ode!, nn, nn_input_fn, u0, tspan, data_times, data_states, p_base; max_iters, lr, ...) -> UpdateResult"},{"category":"section","location":"api/#symbolic_regression_of_nn(nn,-params,-state,-input_ranges;-n_samples,-max_grid_points)-(inputs,-outputs)","page":"API contracts","text":"Sample the trained NN over a grid (single batched Lux.apply; grid capped at max_grid_points). The UpdateResult overload prefers sampling along the fitted trajectory (use_trajectory=true, plus jittered copies) over the grid.","title":"symbolic_regression_of_nn(nn, params, state, input_ranges; n_samples, max_grid_points) -> (inputs, outputs)"},{"category":"section","location":"api/#sparse_regression(inputs,-outputs;-basis_degree3,-extra_basis[],-threshold0.05,-max_sr_iters10,-scale_columnsfalse)-Vector{NamedTuple}","page":"API contracts","text":"STLSQ (SINDy-style) sparse symbolic recovery. extra_basis takes \"name\" => f pairs (f(x::Vector) -> Real) so domain-specific candidate terms stay in the caller's script. Returns per-output (expression, r2, terms).\n\nThis is the symbolic-regression step of the framework, and it is not limited to network corrections: the hook-local characterisation (_characterise_hooks) applies it directly to the measured balance-law residual, with basis_degree = 1 and sin/cos terms supplied through extra_basis.\n\nArgument Default Description\nmax_sr_iters 10 Number of STLSQ prune and refit iterations\nscale_columns false Normalise each basis column to unit RMS before thresholding, so that pruning compares contributions rather than raw coefficients; the returned coefficients are in the original units. Required when the inputs carry different units or ranges, for example a drive of order 10, its derivative of order 0.1 and its unbounded integral, where the threshold would otherwise prune by unit. The hook-local fit sets it to true\n\n","title":"sparse_regression(inputs, outputs; basis_degree=3, extra_basis=[], threshold=0.05, max_sr_iters=10, scale_columns=false) -> Vector{NamedTuple}"},{"category":"section","location":"api/#Supervision","page":"API contracts","text":"","title":"Supervision"},{"category":"section","location":"api/#agent_supervise!(state,-residuals,-drift,-config)-Symbol","page":"API contracts","text":"Returns :continue, :update or :escalate, and mutates state. Call it once per workflow iteration.","title":"agent_supervise!(state, residuals, drift, config) -> Symbol"},{"category":"section","location":"api/#record_update!(state,-result)","page":"API contracts","text":"Log a completed UpdateResult into AgentState.","title":"record_update!(state, result)"},{"category":"section","location":"api/#record_adaptation!(state,-result)","page":"API contracts","text":"Log a completed ModelAdaptationResult into AgentState.adaptation_history.","title":"record_adaptation!(state, result)"},{"category":"section","location":"api/#generate_report(state)-String","page":"API contracts","text":"Full text supervision log including iteration counts, residual history, adaptation count, and log entries.","title":"generate_report(state) -> String"},{"category":"section","location":"api/#explain_for_user(prompt,-config::SupervisionConfig;-timeout-60)-String","page":"API contracts","text":"Ask the LLM configured on the SupervisionConfig (agent_api / agent_api_key / agent_base_url / agent_model) to turn a technical finding into a short plain-language explanation for a non-expert. Returns a bracketed stub string when agent_api === :none, so the result can be printed unconditionally. This is the call behind ModelAdaptationResult.escalation.\n\nSupervisionConfig fields: use_agent (false), agent_api (:none, with the alternatives :anthropic, :openai, :ollama, :groq, :openrouter), agent_api_key, agent_base_url, agent_model, escalate_on_repeated_drift (true), max_auto_updates (3), human_threshold (0.5), ask_human_on_uncertainty (true), and, for :ollama_native adaptation calls only, agent_num_ctx and agent_think (nothing, falling back to the environment variables above).\n\nagent_supervise! evaluates escalate_on_repeated_drift and human_threshold before consulting the agent; the agent cannot override an escalation decided by either.\n\n","title":"explain_for_user(prompt, config::SupervisionConfig; timeout = 60) -> String"},{"category":"section","location":"api/#Top-Level-Workflow","page":"API contracts","text":"","title":"Top-Level Workflow"},{"category":"section","location":"api/#run_airmed(problem,-base_ode!,-nn_input_fn;-kwargs...)-(AgentState,-Union{UpdateResult,-Nothing},-Union{ModelAdaptationResult,-Nothing})","page":"API contracts","text":"Execute one full AIRMED iteration (simulate → compare → detect → supervise → update → optional structural adaptation). Returns a 3-tuple; 2-variable destructuring (state, result = run_airmed(…)) keeps working.\n\nKey keyword arguments:\n\nArgument Default Description\nn_points 200 Data/simulation sampling resolution\nverbose true Print progress to stdout\nnn_input_dim 2 Input dimension of the correction network\nnn_output_dim auto Output dimension (defaults to n_states)\nnn_hidden 16 Hidden layer width\nnn_depth 2 Number of hidden layers\nmax_iters 500 Maximum UDE training iterations\nlr 1e-3 Learning rate for Adam optimiser\nadapt false Run propose_model_adaptation after a successful UDE update\nadapt_api / adapt_api_key / adapt_base_url / adapt_model from supervision_config.agent_* LLM backend for the adaptation step\nadapt_input_ranges auto Ranges for the network characterisation; defaults to the extrema of the fitted trajectory ±5%\nadapt_min_drift_explained 0.25 Skip the adaptation, with a warning, if the UDE explained less than this fraction of the base-model drift, measured by :drift_explained. A correction that did not capture the drift yields an unreliable characterisation. Set 0 to disable the check\nadapt_kwargs (;) NamedTuple forwarded to propose_model_adaptation (e.g. (; max_retries=5))\n\nExplainability principle: the operating digital twin remains the symbolic MTK model, base or adapted. The trained network correction serves as a diagnostic instrument that characterises the cause of a drift so that an explainable physical component can replace it, and it is not simulated as part of the running system. make_ode_problem forwards problem.adaptation_guesses as MTK initialisation guesses, so an adopted adapted model whose initialisation DAE is under-determined can be simulated directly with simulate(problem).\n\nIndex-1 DAE support: models whose structural_simplify result retains algebraic unknowns, i.e. a non-identity mass matrix, are supported throughout. simulate selects Rodas5P when the mass matrix is not the identity, which an explicit solver overrides. run_airmed extracts the trained dynamic states from the solution by symbol in the order of the problem.u0 map, rather than by row position in Array(sol), which also contains the algebraic unknowns. The plain-Julia u0 vector for train_ude is taken from the values of the u0 map, whose order defines the state ordering for base_ode!, as the order of p0 does for its parameters. Such models usually require numeric adaptation_guesses for their algebraic variables, for example branch currents and node voltages, to resolve cyclic symbolic substitutions during MTK initialisation.","title":"run_airmed(problem, base_ode!, nn_input_fn; kwargs...) -> (AgentState, Union{UpdateResult, Nothing}, Union{ModelAdaptationResult, Nothing})"},{"category":"section","location":"api/#propose_model_adaptation:-structure-quality-controls","page":"API contracts","text":"Argument Default Description\ncomplexity_penalty 0.05 Attempts are ranked by fit_loss * (1 + penalty * n_components)\nholdout_fraction 0.25 Tail fraction excluded from fitting and included in validation\nmax_retries 3 Structural retry attempts, triggered by drift, a channel above the noise floor, or a parameter at a bound\nretry_ratio_threshold 10.0 Inert, retained for API compatibility. It is accepted and appears in the failed-attempt text passed to the LLM, but gates nothing. The earlier test fit_loss / ude_loss > threshold required a UDE and gave the wrong verdict when that UDE overfitted, reporting an accurate proposal whose channels sat at their sensor sigma as incomplete. Adequacy is now judged per channel against the declared sensor noise; see Retry and escalation\n\nComponentGuess gained param_min / param_max (default 1e-12/1e12, must be positive): plausibility bounds for the fitted parameter. A fitted value pinned at a bound is reported to the LLM and triggers a structural retry. DriftConfig gained min_sigma (default 1e-8): floor for the baseline σ in CUSUM/EWMA; the adapted-model drift check floors it at 1e-6 × RMS(data) automatically.","title":"propose_model_adaptation: structure-quality controls"},{"category":"section","location":"design/#Design-notes","page":"Design notes","text":"Rationale behind the parts of the pipeline whose behaviour is not evident from the code itself. The source refers to the sections below instead of repeating them inline.","title":"Design notes"},{"category":"section","location":"design/#Hook-local-characterisation","page":"Design notes","text":"Measures the constitutive law of the missing element at each declared hook, rather than inferring it from a global correction.\n\nA global UDE adds one lumped term to a state derivative. It can therefore indicate that the model is incomplete, but not where: with a single observed dynamic state it cannot distinguish topologies with identical effective dynamics. The hook residual can, since it is computed per hook from the sensors.\n\nSteps, per hook:\n\nBalance law from problem.hook_residuals, giving a (drive, response) pair without a model.\nConditioning: smooth, differentiate, integrate, and propagate the sensor noise.\nSNR gate: decide whether a residual is present at all.\nLocal regression on the measured pair, supervised and without an ODE solve or adjoint, at a cost of about 0.5 s.\nSparse fit over a basis derived from component_guesses, with one column per candidate taken from its constitutive equation, so that a surviving coefficient is that component's parameter.\n\nThe basis in step 5 is taken from the library rather than from a generic polynomial. Over a narrow operating range the monomials are near-collinear, so the fit can recover a wrong decomposition with a smaller residual than the true law. The library columns are near-orthogonal on the same data.\n\nImplemented in _characterise_hooks.","title":"Hook-local characterisation"},{"category":"section","location":"design/#Choice-of-candidate-inputs","page":"Design notes","text":"The candidate inputs are every observable channel of the known components, plus time, plus the derivative and the integral of the drive.\n\nOnly the constituents of the response are excluded. The target is their linear combination, so retaining them would let a regressor reproduce it exactly without identifying a law. The drive is kept: it is never one of them, since a parallel hook is driven by a potential and responds with flows, and a series hook the reverse. Dropping it would remove the hypothesis response = f(drive), which is the relevant one for a two-terminal element.\n\nThe derivative and the integral of the drive make energy-storing elements expressible. A capacitor obeys i = C*du/dt and an inductor u = L*di/dt, both relations to the rate of the drive, which a regressor cannot see from instantaneous values alone. Storage does not appear as unequal port currents: in a one-port the two port flows are equal and opposite even for a capacitor, since charge is separated across the dielectric rather than accumulated at a terminal. It appears in the dynamic relation between potential and flow. Unequal port flows indicate a two-port, which is modelled explicitly rather than identified here.\n\nterminal_inputs = true narrows the basis to the terminal quantities of the element itself: its drive, the derivative and integral of that drive, and time. The constitutive law of a two-terminal component relates its own potential, flow and, for a scheduled or cycling element, time; channels of other components do not enter it. Including them can yield a fit in the channels of a slack element that absorbs every imbalance. Such a fit restates conservation instead of a constitutive law, and can reach a higher coefficient of determination than the correct law, after which pruning removes the correct terms.","title":"Choice of candidate inputs"},{"category":"section","location":"design/#Generic-basis-symbolic-regression","page":"Design notes","text":"The sparse fit runs over the live inputs above, i.e. the measured channels of the known components plus time. No component is assumed: the reported law is expressed in quantities the sensors deliver, and the mapping onto a component is left to the model. Regressing on the drive of the element and its derivative and integral is uninformative once that drive is pinned by a stiff bus.\n\nThe basis has degree 1. Degree 2 is not identifiable at this excitation: raw powers of a channel varying by a few percent around a large mean are nearly collinear with the intercept, and STLSQ then returns cancelling terms. The coefficient of determination does not reveal this, since it is similar for the correct fit and for a spurious multi-term fit; the scaled condition number is the diagnostic that does.\n\nA monomial basis in the measured channels cannot express a periodic residual. With time among the inputs the closest approximation is a ramp, so an oscillating and a static element yield the same verdict. If the spectrum of the residual shows a dominant line, the matching sine and cosine pair is added to the basis, with the period measured rather than assumed.\n\nThe pair is added without being announced in the prompt. Reporting it earlier described what was offered rather than what was found: smoothed noise carries enough low-frequency structure for the residual of a constant load to pass the peak gate, so the prompt stated a period for a constant element while the fit classified it as constant. Periodicity is reported after the fit, and only if a trigonometric term survives pruning.","title":"Generic-basis symbolic regression"},{"category":"section","location":"design/#Significance-of-the-fit","page":"Design notes","text":"STLSQ prunes on relative coefficient size and therefore keeps a term whose contribution is small in absolute terms but not relative to a large intercept, which is the case when a spurious channel term and the intercept combine to reproduce a constant. An F-test against the intercept-only null decides whether the fit improves on the constant model; reporting the channel term without it would assert a dependence that the data do not support.\n\nThe effective sample size is used rather than the raw count. _hl_smooth averages over smooth_w samples, so neighbouring residuals are correlated and only about n/w are independent. Using n inflates F by about w and would declare smooth collinear ramps significant against smoothed white noise. _hl_accept_factor applies the same correction.","title":"Significance of the fit"},{"category":"section","location":"design/#Existing-parameter-pre-pass","page":"Design notes","text":"","title":"Existing-parameter pre-pass"},{"category":"section","location":"design/#Per-channel-goodness-of-fit-gate","page":"Design notes","text":"A single existing parameter that explains the drift must fit every observable, not only the cross-channel average. The averaged drift check combines all sensors, so a fit that reproduces all but one channel while being far off on that one still appears clean. This occurs when a new parallel load is absorbed by lowering an existing resistance, which matches the bus voltage and the other branch currents but not the branch current of the component whose parameter was changed.\n\nSuch a fit is rejected when the normalised RMS residual of the worst channel is both non-trivial in absolute terms and an outlier relative to the median channel, which indicates a structural fault that no single parameter can absorb and that should proceed to the structural search. The thresholds are EXISTING_FIT_WORST_CHANNEL_FLOOR and EXISTING_FIT_WORST_CHANNEL_OUTLIER_K.","title":"Per-channel goodness-of-fit gate"},{"category":"section","location":"design/#Exclusion-of-noise-floor-channels","page":"Design notes","text":"A channel whose true trajectory lies at the sensor noise floor, for example an idle branch current that remains near zero for any value of an adaptable parameter, carries no signal that a single-parameter fit could explain. Even an exact fit leaves a residual equal to the noise, and after normalisation by the RMS of that same noise-dominated channel the ratio is approximately 1 regardless of the parameter value, since residual and scale are both noise. This artefact would dominate the per-channel outlier check, so such channels are excluded from it through the signal-to-noise gate. They still contribute to the averaged CUSUM check.\n\nThe noise estimate is formed from first differences, as the mean square of consecutive deltas divided by two. Sample-to-sample noise raises it while a slowly varying trend or a plateau does not, so it serves as a noise estimate without a problem-specific constant. The threshold is EXISTING_FIT_CHANNEL_SNR_MIN.","title":"Exclusion of noise-floor channels"},{"category":"section","location":"design/#Re-anchoring-for-trailing-plateau-segments","page":"Design notes","text":"A plateau candidate receives data_times and data_states already sliced to a trailing segment, so the first sample lies later than problem.tspan[1]. If the ODEProblem were built over the full problem.tspan starting from problem.u0, the solver would integrate the candidate value across the skipped prefix, a period this fit is not scored against, before reaching the segment under test. For a state that integrates its input, such as a state of charge, that prefix covers the earlier transient which the constant candidate value cannot explain, and the accumulated bias enters every compared sample.\n\nThe ODEProblem therefore starts at the first sample of the segment, and each u0 state is re-anchored to its measured value at that sample where the state is one of problem.observable_states; otherwise the entry of problem.u0 is used. For a full-window call this reduces to problem.u0 and problem.tspan.","title":"Re-anchoring for trailing plateau segments"},{"category":"section","location":"design/#Escalation-on-an-inadequate-structure","page":"Design notes","text":"The retry loop retries while the adapted model still drifts, a channel lies above the noise floor, or a fitted parameter reaches a plausibility bound, but it cannot act once the budget is exhausted. In that terminal case the best attempt may still be poor against the noise floor, and without a rejection such a structure is returned with success = true, i.e. a wrong answer is reported with the same confidence as a correct one.\n\nescalate_on_inadequate_structure closes that case. It is disabled by default, since it changes success, message and escalation for callers whose retries end on a poor verdict.\n\nThe second condition covers what the 5 sigma threshold cannot detect. If _fit_proposal_parameters fails before producing a loss, because the model does not build or no fittable parameter exists, it returns with an infinite fit loss and every proposal at its unfitted default. The worst-channel ratio then remains zero, not because the fit is good but because it was never computed, so the threshold reads as adequate and a proposal carrying default parameters would be reported as successful. A fit that could not be attempted is at least as strong an indication of an inadequate structure as one that converged to a poor value.","title":"Escalation on an inadequate structure"},{"category":"section","location":"design/#Rebuilding-the-adapted-twin","page":"Design notes","text":"twin_rebuild.jl converts the diagnosis returned by propose_model_adaptation into a runnable AIRMEDProblem, for example to re-check drift on a fresh window or to compare base and adapted predictions against measurements.\n\nAll required inputs, i.e. the base equations, subsystems, independent variable, component factories, initialisation guesses and solver settings, are available on the AIRMEDProblem used for the adaptation. Domain-specific input is limited to the ComponentGuess factories, the initial guesses, and the choice of data source and observables.\n\nThe wiring is shared with the fitting path through _assemble_adapted_system, so a structural fix is wired identically for fitting and for simulation. Series insertions replace the existing direct connection.","title":"Rebuilding the adapted twin"},{"category":"section","location":"workflow/#AIRMED-Agentic-Workflow","page":"Workflow","text":"","title":"AIRMED Agentic Workflow"},{"category":"section","location":"workflow/#Overview","page":"Workflow","text":"AIRMED runs a closed-loop iterative workflow:\n\nsimulate → compare → detect drift → update model → supervise → repeat\n\nEach iteration is executed by run_airmed(problem, base_ode!, nn_input_fn).\n\n","title":"Overview"},{"category":"section","location":"workflow/#Detailed-Steps","page":"Workflow","text":"","title":"Detailed Steps"},{"category":"section","location":"workflow/#1.-Simulate","page":"Workflow","text":"Run the current MTK model over the configured tspan. The simplified ODE is solved with Tsit5 (configurable via solver= kwarg).","title":"1. Simulate"},{"category":"section","location":"workflow/#2.-Load-/-Generate-Data","page":"Workflow","text":"Call load_data(problem.data_source, tspan, n_points).\n\nFunctionDataSource: synthetic data from a callable, e.g. for tests.\nCSVDataSource: reads measurement data from a CSV file.","title":"2. Load / Generate Data"},{"category":"section","location":"workflow/#3.-Compute-Residuals","page":"Workflow","text":"compute_residuals(sim_times, sim_states, data_times, data_states)   Linearly interpolates the simulation to measurement time points and returns point-wise absolute residuals |sim - meas|.","title":"3. Compute Residuals"},{"category":"section","location":"workflow/#4.-Detect-Drift","page":"Workflow","text":"detect_drift(scalar_residuals, drift_config) dispatches on drift_config.method, a DriftDetectionMethod instance that carries its own parameters:\n\nMethod Key Parameters Notes\nCUSUM(k=.., h=..) k, h CUSUM chart on normalised data\nEWMA(lambda=.., L=..) lambda, L EWMA control chart\nSimpleThreshold(threshold=..) threshold Simple absolute threshold\n\nDriftConfig's remaining fields (min_samples, min_sigma, window_size) are shared baseline-estimation settings, used the same way regardless of method. A new algorithm requires a new DriftDetectionMethod subtype and one _detect method in validation.jl; DriftConfig and detect_drift remain unchanged.\n\nReturns a DriftResult with drift_detected::Bool and the detection index.","title":"4. Detect Drift"},{"category":"section","location":"workflow/#5.-Supervision-Decision","page":"Workflow","text":"agent_supervise!(state, residuals, drift, supervision_config) returns one of:\n\n:continue: no action required\n:update: trigger a model update\n:escalate: require human review\n\nEscalation conditions (configurable):\n\nResidual exceeds human_threshold\nNumber of drift events exceeds max_auto_updates\nLLM agent returns :escalate","title":"5. Supervision Decision"},{"category":"section","location":"workflow/#6.-Model-Update-(UDE-training),-optional-and-diagnostic-only","page":"Workflow","text":"run_update_workflow(...) builds and trains a UDE:\n\nA Lux Chain network is added as an additive correction to base_ode!.\nTraining uses Adam + Optimization.AutoZygote() + InterpolatingAdjoint.\nLoss: MSE between UDE trajectory and measurement data.\n\nAfter training, symbolic_regression_of_nn(...) can sample the learned NN to produce input/output data suitable for SINDy / DataDrivenDiffEq.\n\nThe trained network does not become the operating twin. It serves as a diagnostic instrument for characterising a drift, after which the twin is rebuilt from physical components and the network is discarded. A twin containing a neural correction is not explainable.\n\nThis step is optional. propose_model_adaptation accepts update_result = nothing, and with include_ude_sections = false no UDE content reaches the LLM at all. The structural path below is self-sufficient: it diagnoses from the sensors directly.","title":"6. Model Update (UDE training), optional and diagnostic only"},{"category":"section","location":"workflow/#7.-Structural-Adaptation","page":"Workflow","text":"When the drift is caused by a missing component rather than by a mis-tuned parameter, propose_model_adaptation(problem; …) runs the following sequence:\n\nExisting-parameter pre-pass. Each declared AdaptableParam is re-fitted independently, against the full window and against trailing plateau segments. If one explains the drift, the function returns without an LLM call. Fit quality is judged per observable channel rather than averaged, since an averaged score allows one badly fitted channel to be hidden by the remaining ones.\nEvidence, computed arithmetically from the measured channels, without simulation and without training:\n_characterise_sensor_residuals: which channels deviate from the twin;\nhook_snrs: the balance-law signal-to-noise ratio at each hook, i.e. the location of the fault;\n_characterise_hooks: the properties of the missing element at each active hook, i.e. its operating point, whether its law is static, and a sparse fit over the measured channels.\nPrompt assembly from the problem definition alone. Every component type, port name and library label comes from the caller; the framework contributes no domain vocabulary.\nLLM call, then parse. See LLM backends.\nWinnowing. Proposal subsets are tried in increasing size, and the smallest subset that fits cleanly, i.e. converged, drift cleared and no parameter at a bound, is selected. Winnowing can only remove proposals.\nFit and re-check. The parameters of the selected proposal are optimised, the adapted model is re-simulated, and drift is re-checked.\nVerdict. Per-channel RMSE against the declared sensor noise. A model cannot fit better than its measurement, so a residual at the noise floor is the best attainable result.\nRetry (max_retries, default 3). If the adapted model still drifts, if any channel lies above the noise floor, about 1.12 sigma for a 200-sample window, or if a fitted parameter is at a plausibility bound, the model is asked again. The re-ask consists of the same prompt plus a list of the attempts already made, each with its proposals, fit loss, drift verdict, per-channel RMSE and bound flags. It contains no simulation output and no optimiser trace. Once the budget is spent, the best attempt is selected, ranked by fit_loss * (1 + complexity_penalty * n_components).\nEscalation (opt-in). With escalate_on_inadequate_structure = true, a best attempt whose worst channel remains above 5 sigma, or whose fit produced no loss at all, is rejected rather than returned: success = false, and a separate LLM call fills ModelAdaptationResult.escalation with an account addressed to a human. Disabled by default, so that results remain comparable with earlier runs.\n\nSteps 8 and 9 use different thresholds: the re-ask threshold, about 1.12 sigma, is stricter than the escalation threshold of 5 sigma. A correct proposal slightly above the noise floor is therefore re-asked although it would not escalate. The additional call costs time but cannot degrade the result, since the best attempt is still selected.\n\nThe rebuilt twin is then available through build_fix_problem(problem, result).","title":"7. Structural Adaptation"},{"category":"section","location":"workflow/#8.-Feedback-/-Validation","page":"Workflow","text":"After an update:\n\nThe user (or agent) can re-run the workflow to verify the updated model reduces drift below the configured threshold.\nrecord_update!(state, result) stores the result in AgentState.\ngenerate_report(state) produces a full textual supervision log.\n\n","title":"8. Feedback / Validation"},{"category":"section","location":"workflow/#Human-in-the-Loop","page":"Workflow","text":"Set supervision_config.ask_human_on_uncertainty = true to require human confirmation before updating when residuals are large.\n\nWhen escalation fires, inspect generate_report(state) and decide manually whether to re-tune the UDE, revise the model structure, or accept the deviation.\n\n","title":"Human-in-the-Loop"},{"category":"section","location":"workflow/#LLM-Agent-Integration","page":"Workflow","text":"Set supervision_config.use_agent = true and choose an agent_api:\n\n:anthropic: Anthropic Messages API.\n:openai, :ollama, :groq, :openrouter: all use the OpenAI chat/completions format; agent_base_url selects the server.\n:none: no LLM is called. The supervisor does not escalate via the agent, adaptation falls back to demo-mode enumeration, and explain_for_user returns a stub.\n\nThe LLM receives the current residual statistics and the drift history and answers with \"update\", \"escalate\" or \"continue\". An unrecognised or failed response results in :escalate.\n\nagent_supervise! evaluates escalate_on_repeated_drift and human_threshold before consulting the agent. The agent decides only the cases that neither has already escalated and cannot override them.\n\nAdaptation supports more backends than supervision. propose_model_adaptation additionally accepts :claude_cli, a local Claude Code CLI in print mode, and :ollama_native, the native /api/chat endpoint of Ollama required for reasoning models. The agent_* fields of the supervision config provide the defaults, including agent_num_ctx and agent_think for :ollama_native. See LLM backends.\n\n","title":"LLM Agent Integration"},{"category":"section","location":"workflow/#Configuration-Reference","page":"Workflow","text":"See src/types.jl for all configurable fields on DriftConfig and SupervisionConfig.","title":"Configuration Reference"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"The exported types and functions, grouped by the stage of the workflow they belong to. The hand-written contracts, including the full keyword list of propose_model_adaptation and the meaning of each result field, are in API contracts. Internal helpers are documented in Internals.\n\nPages = [\"reference.md\"]","title":"API reference"},{"category":"section","location":"reference/#Module","page":"API reference","text":"","title":"Module"},{"category":"section","location":"reference/#Problem-definition","page":"API reference","text":"","title":"Problem definition"},{"category":"section","location":"reference/#Data-sources","page":"API reference","text":"","title":"Data sources"},{"category":"section","location":"reference/#Configuration","page":"API reference","text":"","title":"Configuration"},{"category":"section","location":"reference/#Simulation","page":"API reference","text":"","title":"Simulation"},{"category":"section","location":"reference/#Validation-and-drift-detection","page":"API reference","text":"","title":"Validation and drift detection"},{"category":"section","location":"reference/#Model-adaptation","page":"API reference","text":"","title":"Model adaptation"},{"category":"section","location":"reference/#Rebuilding-the-adapted-twin","page":"API reference","text":"","title":"Rebuilding the adapted twin"},{"category":"section","location":"reference/#UDE-training-and-symbolic-regression","page":"API reference","text":"","title":"UDE training and symbolic regression"},{"category":"section","location":"reference/#Supervision","page":"API reference","text":"","title":"Supervision"},{"category":"section","location":"reference/#Top-level-workflow","page":"API reference","text":"","title":"Top-level workflow"},{"category":"module","location":"reference/#AIRMED","page":"API reference","text":"AIRMED: Adaptive Improvement and Realignment of simulation ModEls for Digital twins\n\nA Julia framework for autonomous digital twin simulation with continuous model validation, ML-based adaptation, and agent-in-the-loop supervision.\n\n\n\n\n\n","title":"AIRMED"},{"category":"type","location":"reference/#AIRMED.AIRMEDProblem","page":"API reference","text":"AIRMEDProblem\n\nThe problem definition: the model, its data source, and the detection and supervision settings. Built with the keyword constructor, which simplifies the model and validates the hook declarations.\n\nFields\n\nname, model, simplified_model: identifier and the MTK model, before and after structural_simplify.\ntspan, u0, p0: time span, initial conditions and parameters.\ndata_source: where the measurements come from.\ndrift_config, supervision_config: detection and supervision settings.\ncomponent_hooks, component_guesses: where a component may be inserted and which types are candidates.\nadaptable_params, optimizable_params: parameters that may be re-fitted or calibrated.\nobservable_states, observation_noise: the sensors and their 1 sigma noise.\nhook_residuals: the balance law per hook, for the hook-local step.\nmodel_preamble: using lines written into the generated code.\nport_aliases, component_library_label: naming of the caller's library.\nadaptation_guesses: initial values for an ambiguous initialisation.\n\n\n\n\n\n","title":"AIRMED.AIRMEDProblem"},{"category":"type","location":"reference/#AIRMED.ComponentHook","page":"API reference","text":"ComponentHook\n\nA position in the model at which an unknown component could be inserted, given by the two ports it would connect.\n\nFields\n\nname: identifier of the hook.\nport_a, port_b: attachment points as (subsystem, port). The same subsystem in both means a parallel insertion, two different ones a series insertion. Port names follow the caller's component library.\n\n\n\n\n\n","title":"AIRMED.ComponentHook"},{"category":"type","location":"reference/#AIRMED.ComponentGuess","page":"API reference","text":"ComponentGuess\n\nA candidate component type the model might be missing. The pipeline decides which type goes at which hook; parameter values come from fitting.\n\nFields\n\ncomponent_type: MTK type name.\ndescription: short description, shown in prompts.\nfactory: (name, value) -> ODESystem, builds the component. All library knowledge lives here, so the framework imports no domain package.\nparameter_name: the scalar parameter to fit. Default :p.\ndefault_parameter: starting value for the fit. Default 1.0.\nparam_min, param_max: plausible bounds, both positive since fitting runs in log-space. A value at a bound triggers a structural retry. Default (1e-12, 1e12).\nconnector_names: the two ports of the produced component. Default (:p, :n).\nconnector_guesses: initial values for connector variables, needed when MTK initialisation is cyclic. Default empty.\ncode_template: (name, value) -> String for the generated declaration line. Default nothing, which emits the generic @named form.\n\n\n\n\n\n","title":"AIRMED.ComponentGuess"},{"category":"type","location":"reference/#AIRMED.AdaptableParam","page":"API reference","text":"AdaptableParam\n\nAn existing model parameter that may be re-fitted instead of inserting a new component. propose_model_adaptation tries each one individually before building a prompt or searching the hooks.\n\nFields\n\nname: identifier of the parameter.\nparam: the symbolic parameter, which must appear in problem.p0.\ndefault_value: nominal value, the starting point of the fit.\nparam_min, param_max: plausible bounds, both positive since fitting runs in log-space. Default (1e-12, 1e12).\ndescription: short description for reports and logs.\n\n\n\n\n\n","title":"AIRMED.AdaptableParam"},{"category":"type","location":"reference/#AIRMED.HookResidualSpec","page":"API reference","text":"HookResidualSpec\n\nThe balance law that holds at one ComponentHook if the model is complete. If it does not close, the imbalance is the through- or across-variable of the missing element, measured without simulation. Supplied by the developer, since deriving it would require the connection-set internals of MTK.\n\nFields\n\nhook: name of the hook this describes.\nkind: :parallel for a flow response from a node balance, :series for a potential response from a loop balance.\ndrive: the observable read as the drive.\nresponse_terms: observable => coefficient pairs summed into the response, with the sign convention of the model.\ndescription: short description, shown in prompts.\n\nExample\n\nHookResidualSpec(:load_parallel, :parallel, r_load.v,\n                 [r_grid.i => 1.0, r_pv.i => 1.0, bat.i => -1.0,\n                  r_load.i => -1.0, r_const.i => -1.0],\n                 \"extra path parallel to the consumers\")\n\n\n\n\n\n","title":"AIRMED.HookResidualSpec"},{"category":"type","location":"reference/#AIRMED.FunctionDataSource","page":"API reference","text":"FunctionDataSource\n\nData source from a callable, e.g. for synthetic or test data.\n\nFields\n\ngenerator: called as (tspan, n_points) -> (times, states).\n\n\n\n\n\n","title":"AIRMED.FunctionDataSource"},{"category":"type","location":"reference/#AIRMED.CSVDataSource","page":"API reference","text":"CSVDataSource\n\nData source backed by a CSV file.\n\nFields\n\nfilepath: the file to read.\ntime_column: column holding the time vector.\nstate_columns: columns holding the observed states, in row order.\ninput_columns: optional columns holding inputs.\n\n\n\n\n\n","title":"AIRMED.CSVDataSource"},{"category":"function","location":"reference/#AIRMED.load_data","page":"API reference","text":"load_data(source::FunctionDataSource, tspan, n_points=200) -> (times, states)\n\nGenerate measurement data over tspan by calling source.generator as (tspan, n_points) -> (times, states).\n\nArguments\n\nsource: the generator to call.\ntspan: time span to cover.\nn_points: number of samples. Default 200.\n\nReturns\n\n(times, states), with states as (n_states x n_times).\n\n\n\n\n\nload_data(source::CSVDataSource, tspan, n_points=nothing) -> (times, states)\n\nLoad measurement data from the CSV file of source.\n\nArguments\n\nsource: names the file and its time and state columns.\ntspan: rows outside this span are dropped.\n\nReturns\n\n(times, states), with states as (n_states x n_times).\n\n\n\n\n\n","title":"AIRMED.load_data"},{"category":"type","location":"reference/#AIRMED.DriftConfig","page":"API reference","text":"DriftConfig\n\nConfiguration for drift detection.\n\nFields\n\nmethod: the algorithm and its own parameters, e.g. CUSUM(k=1.0, h=15.0). Default CUSUM().\nwindow_size: samples per detection window. Default 50.\nmin_samples: samples used for the baseline, below which no detection runs. Default 10.\nmin_sigma: lower bound on the baseline standard deviation, which prevents spurious detections at residuals near machine precision. Default 1e-8.\n\n\n\n\n\n","title":"AIRMED.DriftConfig"},{"category":"type","location":"reference/#AIRMED.DriftDetectionMethod","page":"API reference","text":"DriftDetectionMethod\n\nSupertype for the method-specific parameters of a drift detector, one instance of which is held by DriftConfig.method. A new algorithm needs a subtype and a _detect method in validation.jl. The baseline settings shared by all methods stay in DriftConfig.\n\n\n\n\n\n","title":"AIRMED.DriftDetectionMethod"},{"category":"type","location":"reference/#AIRMED.CUSUM","page":"API reference","text":"CUSUM(; k=0.5, h=5.0)\n\nCumulative-sum drift detection. See _cusum_detect.\n\nFields\n\nk: slack in baseline standard deviations, below which a deviation is not accumulated. Default 0.5.\nh: bound on the accumulated evidence that signals drift. Default 5.0.\n\n\n\n\n\n","title":"AIRMED.CUSUM"},{"category":"type","location":"reference/#AIRMED.EWMA","page":"API reference","text":"EWMA(; lambda=0.2, L=3.0)\n\nExponentially-weighted moving-average drift detection. See _ewma_detect.\n\nFields\n\nlambda: smoothing factor. Default 0.2.\nL: control limit, as a multiple of the adjusted baseline standard deviation. Default 3.0.\n\n\n\n\n\n","title":"AIRMED.EWMA"},{"category":"type","location":"reference/#AIRMED.SimpleThreshold","page":"API reference","text":"SimpleThreshold(; threshold=0.1)\n\nFlag drift as soon as a residual exceeds the threshold, without baseline estimation or accumulated evidence. See _threshold_detect.\n\nFields\n\nthreshold: the absolute bound. Default 0.1.\n\n\n\n\n\n","title":"AIRMED.SimpleThreshold"},{"category":"type","location":"reference/#AIRMED.SupervisionConfig","page":"API reference","text":"SupervisionConfig\n\nConfiguration of the supervisor agent_supervise! and of every LLM call the framework makes on its behalf.\n\nThe escalation conditions are checked first; the agent is consulted only if neither fires, and cannot override them.\n\nFields\n\nuse_agent: ask the LLM for the decision when no escalation condition fired. Otherwise that case becomes :update. Default false.\nagent_api: backend, one of :anthropic, :openai, :ollama, :groq, :openrouter, :none. :none calls no LLM anywhere. Default :none.\nagent_api_key: key for that backend; unused for :none and local servers.\nagent_base_url: endpoint override, required for :ollama.\nagent_model: model name; empty selects a per-call default.\nescalate_on_repeated_drift: escalate once the drift events exceed max_auto_updates, capping unattended updates. Default true.\nmax_auto_updates: that limit. Default 3.\nhuman_threshold: residual above which one drift event escalates, if ask_human_on_uncertainty is set. Default 0.5.\nask_human_on_uncertainty: enables the human_threshold check. Default true.\nagent_num_ctx, agent_think: context size and reasoning pass for :ollama_native adaptation calls. nothing uses AIRMED_OLLAMA_NUM_CTX and AIRMED_OLLAMA_THINK. Reasoning shares the token allowance of the answer.\n\n\n\n\n\n","title":"AIRMED.SupervisionConfig"},{"category":"function","location":"reference/#AIRMED.simulate","page":"API reference","text":"simulate(problem::AIRMEDProblem; saveat=nothing, solver=nothing, kwargs...)\n\nSimulate the base model and return the ODE solution.\n\nArguments\n\nproblem: supplies the simplified model, u0, p0 and tspan.\n\nKeywords\n\nsaveat: time points to store.\nsolver: nothing picks Tsit5, or Rodas5P for an index-1 DAE.\nabstol, reltol: solver tolerances.\n\nReturns\n\nThe ODESolution.\n\n\n\n\n\n","title":"AIRMED.simulate"},{"category":"function","location":"reference/#AIRMED.simulate_ude","page":"API reference","text":"simulate_ude(\n    ude!::Function, u0, tspan, p;\n    saveat=nothing, solver=Tsit5(), kwargs...\n)\n\nSimulate a UDE given as a plain Julia ODE function.\n\nArguments\n\nude!: (du, u, p, t), the physics with its correction term.\nu0, tspan, p: initial state, time span and parameters.\n\nKeywords\n\nsaveat: time points to store.\nsolver, abstol, reltol: solver and tolerances.\n\nReturns\n\nThe ODESolution.\n\n\n\n\n\n","title":"AIRMED.simulate_ude"},{"category":"function","location":"reference/#AIRMED.make_ode_problem","page":"API reference","text":"make_ode_problem(problem::AIRMEDProblem) -> ODEProblem\n\nBuild an ODEProblem from the simplified model of problem.\n\nArguments\n\nproblem: supplies the model, u0, p0 and the initialisation guesses.\n\nKeywords\n\ntspan: time span to solve over. Defaults to that of problem.\n\nReturns\n\nThe ODEProblem. A non-empty problem.adaptation_guesses is passed to MTK as initialisation guesses, which an adapted model with an under-determined initialisation DAE requires.\n\n\n\n\n\n","title":"AIRMED.make_ode_problem"},{"category":"function","location":"reference/#AIRMED.interpolate_to_times","page":"API reference","text":"interpolate_to_times(sol, target_times) -> Matrix{Float64}\n\nInterpolate a solution to arbitrary time points.\n\nArguments\n\nsol: the solution to interpolate.\ntarget_times: the time points to evaluate at.\n\nReturns\n\nAn (n_states x n_times) matrix.\n\n\n\n\n\n","title":"AIRMED.interpolate_to_times"},{"category":"function","location":"reference/#AIRMED.model_summary","page":"API reference","text":"model_summary(sys::ODESystem)\n\nPrint the states, parameters and equation count of sys. Returns nothing.\n\n\n\n\n\n","title":"AIRMED.model_summary"},{"category":"function","location":"reference/#AIRMED.get_state_names","page":"API reference","text":"get_state_names(sys::ODESystem) -> Vector{Symbol}\n\nThe names of all state variables of sys.\n\n\n\n\n\n","title":"AIRMED.get_state_names"},{"category":"function","location":"reference/#AIRMED.get_param_names","page":"API reference","text":"get_param_names(sys::ODESystem) -> Vector{Symbol}\n\nThe names of all parameters of sys.\n\n\n\n\n\n","title":"AIRMED.get_param_names"},{"category":"function","location":"reference/#AIRMED.compute_residuals","page":"API reference","text":"compute_residuals(sim_times, sim_states, data_times, data_states) -> (times, residuals)\n\nInterpolate the simulation to the measurement times and compare the two.\n\nArguments\n\nsim_times, sim_states: the simulated trajectory.\ndata_times, data_states: the measurements. Both state arguments are (n_states x n_points) matrices.\n\nReturns\n\n(times, residuals), the measurement times and the point-wise absolute difference.\n\n\n\n\n\n","title":"AIRMED.compute_residuals"},{"category":"function","location":"reference/#AIRMED.detect_drift","page":"API reference","text":"detect_drift(residuals::AbstractMatrix, config::DriftConfig) -> DriftResult\n\nRun drift detection on an (n_states x n_times) residual matrix, averaged across states.\n\nArguments\n\nresiduals: the residual matrix.\nconfig: the detection method and its baseline settings.\n\nReturns\n\nA DriftResult for the averaged series.\n\n\n\n\n\ndetect_drift(residuals::AbstractVector, config::DriftConfig) -> DriftResult\n\nRun drift detection on a scalar residual series.\n\nArguments\n\nresiduals: the series.\nconfig: the detection method and its baseline settings.\n\nReturns\n\nA DriftResult with the verdict, the detection index and the method's own statistics.\n\n\n\n\n\n","title":"AIRMED.detect_drift"},{"category":"type","location":"reference/#AIRMED.DriftResult","page":"API reference","text":"DriftResult\n\nResult of a drift check.\n\nFields\n\ndrift_detected: the verdict.\ndrift_index: first index at which the bound was crossed, or nothing.\nmean_residual, max_residual: statistics of the series checked.\nmethod: which detector produced the result.\ndetails: statistics of that method, e.g. the CUSUM series.\n\n\n\n\n\n","title":"AIRMED.DriftResult"},{"category":"function","location":"reference/#AIRMED.propose_model_adaptation","page":"API reference","text":"propose_model_adaptation(problem, update_result = nothing; kwargs...)\n    -> ModelAdaptationResult\n\nDiagnose a drift as a re-fitted existing parameter or as a missing component. Always returns a ModelAdaptationResult; on failure success = false and message says why.\n\nEach problem.adaptable_params entry is re-fitted first, before any prompt or hook search. If one explains the drift it is returned as a ParameterAdjustment. Otherwise the structural search runs, and _winnow_proposals keeps the smallest subset of a response that still explains the drift.\n\nArguments\n\nproblem: the problem whose model is adapted.\nupdate_result: an UpdateResult from train_ude, or nothing to run without a UDE.\n\nKeywords\n\ndata_times, data_states: the measurements, required for fitting.\napi, api_key, base_url, model: the LLM backend. See the API contracts for the supported values.\nnum_ctx, think: :ollama_native only; nothing uses the environment.\nllm_timeout, llm_log_file: seconds per call, and a file to log prompts to.\nfit_params: fit each proposed parameter to the data. Default true.\nfit_iters, fit_lr, optimization_algorithm: fitting settings. LBFGS is tried first, so fit_lr applies to the Adam fallback.\nholdout_fraction: tail withheld from fitting, used for validation.\ncomplexity_penalty: ranks attempts by fit_loss * (1 + complexity_penalty * n_components); 0 ranks by raw loss.\nmax_retries: re-proposal attempts after the first. Default 3.\nhook_local_analysis, hook_summary, hook_terminal_inputs: control the hook-local characterisation.\nrestrict_hooks_by_snr, hook_snr_min: offer only hooks above an SNR.\ninclude_component_equations, include_ude_sections: prompt content.\nescalate_on_inadequate_structure, explain_on_failure: reject an inadequate result instead of returning it.\nbuild_model: build the generated code into an ODESystem.\ninput_ranges, n_samples: grid for the UDE characterisation.\n\nReturns\n\nA ModelAdaptationResult, also on failure, where success is false and message says why.\n\n\n\n\n\n","title":"AIRMED.propose_model_adaptation"},{"category":"type","location":"reference/#AIRMED.ModelAdaptationResult","page":"API reference","text":"ModelAdaptationResult\n\nResult of propose_model_adaptation.\n\nFields\n\nproposals: components to insert; empty for a parameter-only fix.\nparameter_adjustments: parameters re-fitted in place; empty for a structural fix.\ngenerated_code: MTK source for the adapted system, regenerated from the final proposals rather than taken from the response.\nadapted_model: the built ODESystem, or nothing.\nprompt, raw_response: those of the accepted attempt.\nconversation: every attempt in order, including discarded ones.\napi: the backend used.\nsuccess: whether the pipeline produced a usable result.\nmessage: status summary, including the structure-quality verdict.\nfit_loss: MSE after fitting, NaN if fitting was skipped or failed.\nescalation: explanation for a human, empty on ordinary paths.\n\n\n\n\n\n","title":"AIRMED.ModelAdaptationResult"},{"category":"type","location":"reference/#AIRMED.ComponentProposal","page":"API reference","text":"ComponentProposal\n\nOne concrete insertion: which component type connects which two ports. Unlike ComponentGuess, this is the outcome of an analysis.\n\nFields\n\nname: variable name used in the generated code.\ncomponent_type: must match a declared ComponentGuess.\nport_a, port_b: attachment points, mapped to the two connector_names.\nparameters: parameter values by symbol.\nrationale: justification given for the proposal.\n\n\n\n\n\n","title":"AIRMED.ComponentProposal"},{"category":"type","location":"reference/#AIRMED.ParameterAdjustment","page":"API reference","text":"ParameterAdjustment\n\nA re-fit of an existing parameter. The structure is unchanged and only the value is corrected.\n\nFields\n\nname, param: taken from the originating AdaptableParam.\nold_value: the value in problem.p0 before the adjustment.\nnew_value: the fitted value.\nrationale: justification for the adjustment.\n\n\n\n\n\n","title":"AIRMED.ParameterAdjustment"},{"category":"type","location":"reference/#AIRMED.LLMExchange","page":"API reference","text":"LLMExchange\n\nOne prompt and response pair. propose_model_adaptation may call the LLM up to max_retries times, and keeps every exchange here, not only the accepted one.\n\nFields\n\nattempt: attempt index, starting at 1.\nprompt, raw_response: what was sent and received, verbatim.\nn_parsed: number of proposals parsed from the response.\nnote: outcome, e.g. accepted, retried or discarded.\n\n\n\n\n\n","title":"AIRMED.LLMExchange"},{"category":"function","location":"reference/#AIRMED.model_adaptation_summary","page":"API reference","text":"model_adaptation_summary(result::ModelAdaptationResult) -> String\n\nSummarise a result: the proposed components, the parameter adjustments and the generated code.\n\nArguments\n\nresult: the result to summarise.\n\nReturns\n\nThe summary as a multi-line string.\n\n\n\n\n\n","title":"AIRMED.model_adaptation_summary"},{"category":"function","location":"reference/#AIRMED.explain_for_user","page":"API reference","text":"explain_for_user(prompt::AbstractString, config::SupervisionConfig) -> String\n\nTurn a technical finding into a short plain-language explanation.\n\nArguments\n\nprompt: the finding to explain.\nconfig: supplies the backend through its agent_* fields.\ntimeout: seconds to wait for the response. Default 60.\n\nReturns\n\nThe explanation, or a bracketed stub when no backend is configured, so the result can be printed unconditionally.\n\n\n\n\n\n","title":"AIRMED.explain_for_user"},{"category":"function","location":"reference/#AIRMED.build_fix_problem","page":"API reference","text":"build_fix_problem(problem, adapt_result; kwargs...) -> (prob | nothing, msg, comps | nothing)\n\nTurn either form of a ModelAdaptationResult into a runnable problem: a parameter fix or a structural fix, the former taking precedence when both are present.\n\nReturns\n\n(prob, msg, comps). comps is nothing for a parameter fix, since no component is created.\n\n\n\n\n\n","title":"AIRMED.build_fix_problem"},{"category":"function","location":"reference/#AIRMED.build_adapted_problem","page":"API reference","text":"build_adapted_problem(problem, proposals; kwargs...) -> (prob | nothing, msg, comps | nothing)\n\nThe structural fix as a runnable problem: the model with proposals inserted, every other setting inherited.\n\nKeywords\n\nname: name of the rebuilt problem.\ndata_source: replay source; nothing keeps the original.\nobservable_states: narrow this to judge the twin on fewer channels.\ninit_vals: initial parameter values, as in build_adapted_system.\n\nReturns\n\n(prob, msg, comps), or (nothing, msg, nothing) if the system cannot be built.\n\n\n\n\n\n","title":"AIRMED.build_adapted_problem"},{"category":"function","location":"reference/#AIRMED.build_parameter_adjusted_problem","page":"API reference","text":"build_parameter_adjusted_problem(problem, adjustments; kwargs...) -> (prob, msg)\n\nThe parameter-only fix as a runnable problem: identical topology, with each adjusted value replaced in p0.\n\nArguments\n\nproblem: the problem to rebuild.\nadjustments: the re-fitted parameters to apply.\n\nKeywords\n\nname, data_source, observable_states: as in build_adapted_problem.\n\nReturns\n\n(prob, msg). Adjustments naming a parameter absent from p0 are skipped and reported in msg.\n\n\n\n\n\n","title":"AIRMED.build_parameter_adjusted_problem"},{"category":"function","location":"reference/#AIRMED.build_adapted_system","page":"API reference","text":"build_adapted_system(problem, proposals; init_vals = nothing)\n    -> (sys | nothing, guesses, msg, comps | nothing)\n\nRebuild the model of problem with proposals inserted, returning the unsimplified ODESystem, since AIRMEDProblem simplifies it itself and the raw system still allows symbolic indexing such as sol[load.i].\n\nArguments\n\nproblem: the problem to rebuild from.\nproposals: components to insert.\ninit_vals: initial parameter values. nothing uses each proposal's fitted value, falling back to ComponentGuess.default_parameter.\n\nReturns\n\n(sys, guesses, msg, comps), where comps holds the created components in proposal order and sys is nothing on failure.\n\n\n\n\n\n","title":"AIRMED.build_adapted_system"},{"category":"function","location":"reference/#AIRMED.rebuild_problem","page":"API reference","text":"rebuild_problem(problem; kwargs...) -> AIRMEDProblem\n\nCopy problem, overriding only the fields named in kwargs.\n\nArguments\n\nproblem: the problem to copy.\n\nKeywords\n\nAny field of AIRMEDProblem, e.g. model, data_source or observable_states. Fields not named are carried over, so replacing the model retains the solver window, the initial conditions and the drift settings.\n\nReturns\n\nThe copied problem.\n\n\n\n\n\n","title":"AIRMED.rebuild_problem"},{"category":"function","location":"reference/#AIRMED.build_nn","page":"API reference","text":"build_nn(input_dim::Int, hidden_dim::Int, output_dim::Int; depth=2) -> Lux.Chain\n\nConstruct a dense tanh network for use as a UDE correction.\n\nArguments\n\ninput_dim, hidden_dim, output_dim: layer widths.\n\nKeywords\n\ndepth: number of hidden layers. Default 2.\n\nReturns\n\nThe network, a Lux.Chain of dense tanh layers with a linear output.\n\n\n\n\n\n","title":"AIRMED.build_nn"},{"category":"function","location":"reference/#AIRMED.train_ude","page":"API reference","text":"train_ude(\n    base_ode!::Function,\n    nn, nn_input_fn::Function,\n    u0, tspan, data_times, data_states,\n    p_base;\n    max_iters=500, lr=1e-3, rng=default_rng(), verbose=false,\n    normalize=true, patience=100, loss_target=0.0,\n    polish=true, polish_iters=100,\n) -> UpdateResult\n\nTrain a UDE by minimising the MSE between its trajectory and the measurements. The iterate with the lowest loss is returned, not the last one.\n\nArguments\n\nbase_ode!: (du, u, p_base, t), the known physics.\nnn: the correction network.\nnn_input_fn: (u, t) -> Vector, the network inputs.\nu0, tspan: initial state and time span.\ndata_times, data_states: measurements, (n_states x n_times).\np_base: parameters passed to base_ode!, not differentiated.\n\nKeywords\n\nmax_iters: Adam iterations. <= 0 returns the untrained network with the loss at initialisation. Default 500.\nlr: Adam learning rate. Default 1e-3.\nnormalize: wrap the network with data-derived scaling. Default true.\npatience: stop after this many iterations without improvement.\nloss_target: stop once the loss falls below it.\npolish, polish_iters: LBFGS polish after Adam, ignored on failure.\nsolver, rng, verbose: solver, random seed and progress output.\n\nReturns\n\nAn UpdateResult holding the trained network, the best loss, the network inputs along the fitted trajectory and the per-state RMSE metrics.\n\n\n\n\n\n","title":"AIRMED.train_ude"},{"category":"function","location":"reference/#AIRMED.train_symbolic_ude","page":"API reference","text":"train_symbolic_ude(\n    ude_prob, sym_nn, θ, data_times, data_states;\n    extra_params=[], solver=Tsit5(), max_iters=5000, lr=0.01, verbose=false\n) -> UpdateResult\n\nTrain a UDE whose unknown term is a @SymbolicNeuralNetwork network, using AutoForwardDiff for the gradients.\n\nArguments\n\nude_prob: ODEProblem whose equations already contain sym_nn(x, θ).\nsym_nn, θ: the symbolic network and its parameters.\ndata_times, data_states: the measurements.\n\nKeywords\n\nextra_params: further parameters to fit alongside θ.\nsolver, max_iters, lr, verbose: solver and optimiser settings.\n\nReturns\n\nAn UpdateResult whose sym_nn, sym_theta and fitted_ode_prob fields evaluate the learned function: result.fitted_ode_prob.ps[result.sym_nn](x, result.fitted_ode_prob.ps[result.sym_theta]).\n\n\n\n\n\n","title":"AIRMED.train_symbolic_ude"},{"category":"function","location":"reference/#AIRMED.run_update_workflow","page":"API reference","text":"run_update_workflow(\n    problem::AIRMEDProblem,\n    drift_result::DriftResult,\n    data_times, data_states;\n    nn_hidden=16, nn_depth=2, kwargs...\n) -> UpdateResult\n\nBuild a correction network and train it against the measurements, the update step that follows a detected drift.\n\nArguments\n\nproblem: the problem being updated.\ndrift_result: the detection that triggered the update.\nbase_ode!, nn_input_fn, u0: the known physics, the network inputs and the initial state.\ndata_times, data_states: the measurements.\n\nKeywords\n\nnn_input_dim, nn_output_dim, nn_hidden, nn_depth: network shape.\nkwargs...: forwarded to train_ude.\n\nReturns\n\nThe UpdateResult of the training.\n\n\n\n\n\n","title":"AIRMED.run_update_workflow"},{"category":"type","location":"reference/#AIRMED.UpdateResult","page":"API reference","text":"UpdateResult\n\nResult of a model update attempt.\n\nFields\n\nsuccess, final_loss, n_iterations, message: outcome of the training.\nnn, trained_nn_params, trained_nn_state: the trained network.\nsym_nn, sym_theta, fitted_ode_prob: set only on the symbolic path of train_symbolic_ude, otherwise nothing.\ntrajectory_inputs: network inputs along the fitted trajectory, so later sampling stays on the visited manifold instead of a uniform grid.\nmetrics: diagnostics such as :per_state_rmse_base, :per_state_rmse_ude and :drift_explained, the fraction of base-model residual removed.\n\n\n\n\n\n","title":"AIRMED.UpdateResult"},{"category":"function","location":"reference/#AIRMED.symbolic_regression_of_nn","page":"API reference","text":"symbolic_regression_of_nn(\n    nn, nn_params, nn_state, input_ranges::Vector;\n    n_samples=200, max_grid_points=20_000\n) -> (inputs, outputs)\n\nSample the trained network on a grid, for symbolic regression with sparse_regression.\n\nArguments\n\nnn, nn_params, nn_state: the trained network.\ninput_ranges: (lo, hi) per input dimension.\n\nKeywords\n\nn_samples: points per dimension. Default 200.\nmax_grid_points: cap on the total grid, which lowers the per-dimension resolution as needed. Default 20_000.\n\nReturns\n\n(inputs, outputs) of size (n_inputs x n_points) and (n_outputs x n_points), evaluated in one batched call.\n\n\n\n\n\nsymbolic_regression_of_nn(update_result, input_ranges;\n                          n_samples=200, use_trajectory=true, jitter=0.05)\n\nSample the network of an UpdateResult.\n\nWith use_trajectory = true and available trajectory_inputs, the sample points are those visited along the fitted trajectory plus two jittered copies, clamped to input_ranges. This avoids the off-manifold regions of a uniform grid, where the network extrapolates. Otherwise the grid is used.\n\nKeywords\n\nn_samples: points per dimension for the grid fallback. Default 200.\nuse_trajectory: prefer the fitted trajectory. Default true.\njitter: relative spread of the jittered copies. Default 0.05.\n\nReturns\n\n(inputs, outputs) of size (n_inputs x n_points) and (n_outputs x n_points).\n\n\n\n\n\n","title":"AIRMED.symbolic_regression_of_nn"},{"category":"function","location":"reference/#AIRMED.sparse_regression","page":"API reference","text":"sparse_regression(inputs, outputs;\n                  basis_degree=3, extra_basis=Pair{String,Function}[],\n                  threshold=0.05, max_sr_iters=10)\n    -> Vector{NamedTuple}\n\nSequentially-thresholded least squares (the SINDy core) over a polynomial basis, recovering a sparse expression per output channel.\n\nArguments\n\ninputs: (n_inputs x n_points) samples, e.g. from symbolic_regression_of_nn.\noutputs: (n_outputs x n_points) values at those samples.\n\nKeywords\n\nbasis_degree: maximum total degree of the monomials. Default 3.\nextra_basis: additional candidate terms as \"name\" => f pairs with f(x) -> Real, which keeps domain knowledge in the caller's script.\nthreshold: coefficients below threshold * max|c| are dropped and the rest refitted. Default 0.05.\nmax_sr_iters: prune and refit iterations. Default 10.\nscale_columns: normalise the basis columns to unit RMS first, so pruning compares contributions rather than units. Default false.\n\nReturns\n\nOne entry per output channel with expression, r2 and terms.\n\n\n\n\n\n","title":"AIRMED.sparse_regression"},{"category":"function","location":"reference/#AIRMED.model_update_summary","page":"API reference","text":"model_update_summary(result::UpdateResult; input_ranges=nothing, n_samples=50) -> String\n\nSummarise a trained update as text: status, loss, architecture and parameter count.\n\nArguments\n\nresult: the update to summarise.\n\nKeywords\n\ninput_ranges: one (lo, hi) per input dimension. Given, the summary also reports the range of the learned correction.\nn_samples: grid points used for that range. Default 50.\n\nReturns\n\nThe summary as a multi-line string.\n\n\n\n\n\n","title":"AIRMED.model_update_summary"},{"category":"function","location":"reference/#AIRMED.add_nn_to_ode","page":"API reference","text":"add_nn_to_ode(base_ode!::Function, nn, nn_state_ref::Ref, input_fn::Function)\n\nWrap an ODE function with an additive network correction.\n\nArguments\n\nbase_ode!: (du, u, p_base, t), the known physics.\nnn: the correction network.\nnn_state_ref: holds the Lux state from Lux.setup.\ninput_fn: (u, t) -> Vector, the network inputs.\n\nReturns\n\nAn ODE function (du, u, p, t) whose p is a ComponentArray with a nn field holding the network parameters.\n\n\n\n\n\n","title":"AIRMED.add_nn_to_ode"},{"category":"type","location":"reference/#AIRMED.AgentState","page":"API reference","text":"AgentState\n\nRuntime state of the supervisor, accumulated across iterations.\n\nFields\n\nn_iterations, n_drift_events, n_updates: counters.\nescalated: whether an escalation has occurred.\nresidual_history, drift_history: one entry per iteration.\nupdate_history, adaptation_history: the recorded results.\nlog: timestamped log lines.\n\n\n\n\n\n","title":"AIRMED.AgentState"},{"category":"function","location":"reference/#AIRMED.agent_supervise!","page":"API reference","text":"agent_supervise!(\n    state::AgentState,\n    residuals::AbstractVector,\n    drift::DriftResult,\n    config::SupervisionConfig\n) -> Symbol\n\nRun one supervision step and update state.\n\nArguments\n\nstate: supervision state, mutated in place.\nresiduals: residuals of the current window.\ndrift: result of the drift check.\nconfig: thresholds and agent settings.\n\nReturns\n\n:continue, :update or :escalate.\n\n\n\n\n\n","title":"AIRMED.agent_supervise!"},{"category":"function","location":"reference/#AIRMED.record_update!","page":"API reference","text":"record_update!(state::AgentState, result::UpdateResult)\n\nRecord a completed model update.\n\nArguments\n\nstate: supervision state, mutated in place.\nresult: the update to record. A successful one also raises n_updates.\n\nReturns\n\nThe updated state.\n\n\n\n\n\n","title":"AIRMED.record_update!"},{"category":"function","location":"reference/#AIRMED.record_adaptation!","page":"API reference","text":"record_adaptation!(state::AgentState, result::ModelAdaptationResult)\n\nRecord a completed adaptation.\n\nArguments\n\nstate: supervision state, mutated in place.\nresult: the adaptation to record.\n\nReturns\n\nThe updated state.\n\n\n\n\n\n","title":"AIRMED.record_adaptation!"},{"category":"function","location":"reference/#AIRMED.generate_report","page":"API reference","text":"generate_report(state::AgentState) -> String\n\nReport the state of the supervision as text.\n\nArguments\n\nstate: the state to report.\n\nReturns\n\nThe report: iteration counts, drift events, updates, adaptations, the residual history and the log.\n\n\n\n\n\n","title":"AIRMED.generate_report"},{"category":"function","location":"reference/#AIRMED.run_airmed","page":"API reference","text":"run_airmed(problem, base_ode!, nn_input_fn; kwargs...)\n    -> (AgentState, Union{UpdateResult, Nothing}, Union{ModelAdaptationResult, Nothing})\n\nRun one iteration of the workflow: load data, simulate, compute residuals, detect drift, ask the supervisor, then optionally train a UDE and propose a structural adaptation.\n\nThe network correction is diagnostic only. The operating twin remains the symbolic MTK model, base or adapted.\n\nArguments\n\nproblem: the problem to check.\nbase_ode!: (du, u, p_base, t), the known physics in plain Julia, so that it can be differentiated during UDE training.\nnn_input_fn: (u, t) -> Vector, the network inputs.\n\nKeywords\n\nn_points: samples per window. Default 200.\nverbose: print progress. Default true.\nnn_input_dim, nn_output_dim, nn_hidden, nn_depth: network shape.\ntrain_ude: train a UDE when the supervisor decides :update. Default false, so the adaptation runs on hook-local and sensor evidence alone and the returned UpdateResult slot stays nothing.\nadapt: run propose_model_adaptation afterwards. Default false.\nadapt_api, adapt_api_key, adapt_base_url, adapt_model, adapt_num_ctx, adapt_think: LLM settings for that step. nothing takes the agent_* field of problem.supervision_config.\nadapt_input_ranges: ranges for the network characterisation. nothing derives them from the fitted trajectory.\nadapt_min_drift_explained: skip the adaptation when a trained UDE explained less of the drift than this. Default 0.25, 0 disables the check.\nadapt_kwargs: forwarded to propose_model_adaptation, e.g. (; max_retries = 5).\nstate: reuse a previous AgentState to accumulate history across calls.\ntrain_kwargs...: forwarded to train_ude.\n\nReturns\n\n(state, update_result, adaptation_result). The second is nothing without UDE training or without an update, the third unless the adaptation ran. Two-variable destructuring remains valid.\n\n\n\n\n\n","title":"AIRMED.run_airmed"},{"category":"section","location":"","page":"Home","text":"<p align=\"center\">\n  <img src=\"assets/logo.svg\" alt=\"AIRMED.jl\" width=\"480\">\n</p>","title":"Home"},{"category":"section","location":"#AIRMED.jl","page":"Home","text":"A Julia framework for self-adapting, explainable digital twins. AIRMED compares a ModelingToolkit model against measurements, detects when the two have drifted apart, diagnoses the cause from the sensors, and proposes a physical component with fitted parameters that closes the gap.\n\nnote: Explainability principle\nThe operating twin remains a symbolic MTK model. Neural corrections are used only as diagnostic instruments, to characterise a drift so that an explainable physical component can replace them. A network is never deployed as part of the running system, since a twin containing a neural correction is not explainable.","title":"AIRMED.jl"},{"category":"section","location":"#Workflow","page":"Home","text":"simulate -> compare to data -> detect drift\n              | drift\n     re-fit an existing parameter?  --yes--> done, without an LLM call\n              | no\n     locate it             (balance-law residual and SNR per hook)\n     characterise it       (static-law test, switching, periodicity, sparse fit)\n              |\n     request a component from the caller's library\n              |\n     fit its parameters -> re-simulate -> re-check drift\n              | still inadequate\n     re-ask, listing the failed attempts (up to `max_retries`)\n              | budget exhausted and still inadequate\n     escalate to a human, with the measured evidence\n\nThree properties distinguish this from querying a model directly:\n\nThe simplest explanation is tried first. A drifted existing parameter is diagnosed as such by one local optimisation, without an LLM call and without adding a component.\nThe evidence is measured, not learned. The hook-local characterisation is classical numerics on measured channels: a conservation-law residual, its signal-to-noise ratio against the declared sensor noise, a nearest-neighbour test for hidden state, a periodogram and an STLSQ sparse fit.\nVerdicts are per channel, against sensor noise. A model cannot fit better than its measurement, so a residual at the noise floor is the best attainable result.","title":"Workflow"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.develop(url = \"https://github.com/cgutsche/AIRMED.jl\")\n\nFrom a clone of the repository:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate()'\njulia --project=. -e 'using Pkg; Pkg.test()'\n\nThe test suite requires no LLM and no network.","title":"Installation"},{"category":"section","location":"#A-minimal-problem","page":"Home","text":"using AIRMED, ModelingToolkit\nusing ModelingToolkitStandardLibrary.Electrical\n\nproblem = AIRMEDProblem(;\n    name  = \"RC circuit\",\n    model = base_circuit,\n\n    component_hooks = [\n        ComponentHook(:r1_parallel, (:r1, :p), (:r1, :n), \"parallel to R1\"),\n    ],\n    component_guesses = [\n        ComponentGuess(:Resistor, \"Standard library resistor\",\n                       (name, val) -> Resistor(; R = val, name = name);\n                       parameter_name = :R),\n    ],\n    adaptable_params = [AdaptableParam(:r1_R, r1.R, 1000.0, \"R1 ageing\")],\n\n    model_preamble    = [\"using ModelingToolkitStandardLibrary.Electrical\"],\n    data_source       = FunctionDataSource(measurements),\n    tspan             = (0.0, 0.05),\n    u0                = [cap.v => 0.0],\n    p0                = [r1.R => 1000.0, cap.C => 1e-3, vref.k => 5.0],\n    observable_states = [cap.v],\n    observation_noise = [1e-3],\n)\n\nresult = propose_model_adaptation(problem;\n    data_times = t, data_states = x,\n    api = :ollama_native, model = \"granite4.2:8b-ctx8k\")\n\nfixed_problem, msg, components = build_fix_problem(problem, result)","title":"A minimal problem"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"workflow.md\", \"api.md\", \"design.md\", \"reference.md\"]\nDepth = 2","title":"Contents"},{"category":"section","location":"#Evaluation","page":"Home","text":"Case studies, benchmarks, plotting scripts and recorded run data are maintained in the separate AIRMED_Benchmarks repository, which depends on this package.","title":"Evaluation"},{"category":"section","location":"#License","page":"Home","text":"MIT.","title":"License"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"Helpers that are not exported. They are documented because the adaptation pipeline is easier to follow with them, but they are not part of the public interface and may change without notice. The reasoning behind the analysis steps is in Design notes.\n\nPages = [\"internals.md\"]","title":"Internals"},{"category":"method","location":"internals/#AIRMED._assemble_adapted_system-Tuple{Vector{ComponentProposal}, Vector{Float64}, AIRMEDProblem}","page":"Internals","text":"_build_adapted_ode_system(proposals, init_vals, problem)\n    -> (ODESystem | Nothing, Vector{Pair{Int,Any}}, String)\n\nBuild the adapted ODESystem from the equation objects of the base model, creating components through the factory of the matching ComponentGuess.\n\nThe same subsystem in both ports inserts in parallel, adding two connect equations; two different ones insert in series, replacing the existing connection.\n\nReturns\n\n(sys, param_syms, msg), with sys the simplified system or nothing, and param_syms mapping each proposal index to its symbolic parameter.\n\n\n\n\n\n","title":"AIRMED._assemble_adapted_system"},{"category":"method","location":"internals/#AIRMED._auto_input_ranges-Tuple{UpdateResult}","page":"Internals","text":"_auto_input_ranges(result::UpdateResult) -> Union{Nothing, Vector{Tuple{Float64,Float64}}}\n\nCharacterisation ranges derived from the inputs visited along the fitted trajectory, with a 5% margin per dimension.\n\nArguments\n\nresult: the training result to read trajectory_inputs from.\n\nReturns\n\nOne (lo, hi) per input dimension, or nothing when the trajectory is unavailable, in which case the caller skips the characterisation.\n\n\n\n\n\n","title":"AIRMED._auto_input_ranges"},{"category":"method","location":"internals/#AIRMED._build_adaptation_prompt-Tuple{AIRMEDProblem, Union{Nothing, UpdateResult}, AbstractString}","page":"Internals","text":"_build_adaptation_prompt(problem, update_result, nn_summary;\n                         sensor_summary=\"\") -> String\n\nProduce the natural-language prompt sent to the LLM. The prompt asks for a JSON response with the following schema:\n\n{\n    \"proposals\": [\n        {\n            \"hook\": \"<ComponentHook name from problem.component_hooks>\",\n            \"name\": \"<julia identifier for the new component>\",\n            \"component_type\": \"<one of problem.component_guesses[].component_type>\",\n            \"rationale\": \"...\"\n        }\n    ],\n    \"julia_model_code\": \"...optional, MTK source for the adapted system...\"\n}\n\nParameter values are not requested; they are identified by optimisation.\n\nKeywords\n\nsensor_summary, hook_summary: pre-computed sections; \"\" omits them.\nallowed_hooks: restrict the offered hook names.\ninclude_ude_sections: include the UDE architecture, loss and characterisation.\ninclude_component_equations: list the constitutive equations of each type.\nfailed_attempts: texts of earlier attempts, added on a retry.\n\nThe prompt carries no per-hook relevance ranking; see _score_hooks_by_residuals for why.\n\nReturns\n\nThe prompt as a string.\n\n\n\n\n\n","title":"AIRMED._build_adaptation_prompt"},{"category":"method","location":"internals/#AIRMED._call_claude_cli-Tuple{AbstractString}","page":"Internals","text":"_call_claude_cli(prompt; model, cli_path, workdir, isolation_args, hooks,\n                 aliases, default_component_type, timeout)\n    -> (raw_text, proposals, code)\n\nLLM backend that runs a local Claude Code CLI in print mode, with the prompt on stdin, and parses its stdout like any other free-text backend.\n\nA CLI invocation is an agent session, so it would otherwise add context the framework did not author: CLAUDE.md files, memory, settings, skills, plugins, hooks, MCP servers and file and web tools. The isolation flags and the neutral working directory suppress all of it, leaving the call equivalent to the HTTP backends.\n\nKeywords\n\nmodel: passed as --model; an alias or a full id, omitted when empty.\ncli_path: executable path; empty uses AIRMED_CLAUDE_CLI, else PATH.\nworkdir: working directory, a temporary one by default, which bounds file access and memory discovery.\nisolation_args: the isolation flags; override only if a CLI version rejects one.\ntimeout: seconds before the child is killed.\n\nReturns\n\n(raw_text, proposals, code). On failure the text starts with [claude_cli error, and stderr is surfaced only then.\n\n\n\n\n\n","title":"AIRMED._call_claude_cli"},{"category":"method","location":"internals/#AIRMED._call_llm_text-Tuple{Symbol, AbstractString, AbstractString}","page":"Internals","text":"_call_llm_text(api, prompt, api_key; base_url, model, timeout, log_file) -> String\n\nFree-text LLM call for an explanation rather than a proposal, reusing the backend dispatch of _call_llm. Used so that an escalation is explained by the backend that produced the rejected proposal, unlike explain_for_user, which uses the one in SupervisionConfig.\n\nArguments\n\napi, api_key: the backend and its key.\nprompt: the question to answer.\n\nKeywords\n\nbase_url, model: endpoint and model name.\nnum_ctx, think: :ollama_native only.\ntimeout: seconds per call.\nlog_file: file to append the exchange to.\n\nReturns\n\nThe raw response text.\n\n\n\n\n\n","title":"AIRMED._call_llm_text"},{"category":"method","location":"internals/#AIRMED._call_ollama_native-Tuple{AbstractString}","page":"Internals","text":"_call_ollama_native(prompt; base_url, model, num_ctx, think, ...) -> (raw_text, proposals, code)\n\nThe native /api/chat endpoint of Ollama, used instead of its OpenAI-compatible shim for models that reason before answering. Only this endpoint honours think and options.num_ctx; the shim ignores the first and allocates the declared maximum context.\n\nArguments\n\nprompt: the prompt to send.\n\nKeywords\n\nbase_url, model: endpoint and model name.\nnum_ctx: context size. nothing uses AIRMED_OLLAMA_NUM_CTX.\nthink: reasoning pass. nothing uses AIRMED_OLLAMA_THINK.\nhooks, aliases, default_component_type: passed to the parser.\ntimeout: seconds before the request is abandoned.\n\nReturns\n\n(raw_text, proposals, code).\n\n\n\n\n\n","title":"AIRMED._call_ollama_native"},{"category":"method","location":"internals/#AIRMED._characterise_hooks-Tuple{AIRMEDProblem, Any, Any}","page":"Internals","text":"_characterise_hooks(problem, data_times, data_states; include_analysis=true) -> String\n\nPer-hook constitutive measurement, formatted for the prompt. Every step is classical numerics on the measured channels: a moving average, a nearest-neighbour test for hidden state, an Otsu split, a periodogram and sequentially-thresholded least squares. No network is involved.\n\nArguments\n\nproblem: supplies hook_residuals, observation_noise and the observables.\ndata_times, data_states: the measurements.\n\nKeywords\n\ninclude_analysis: with false, only the balance-law residual and its SNR are reported. Default true.\nsmooth_w: width of the moving average. Default 15.\nonly_hooks: restrict the report to these hooks.\nterminal_inputs: fit in the element's own terminal quantities.\n\nReturns\n\nA multi-line string, empty when the problem declares no hook residuals or no observation noise.\n\n\n\n\n\n","title":"AIRMED._characterise_hooks"},{"category":"method","location":"internals/#AIRMED._characterise_sensor_residuals-Tuple{AIRMEDProblem, AbstractVector, AbstractMatrix}","page":"Internals","text":"_characterise_sensor_residuals(problem, data_times, data_states) -> String\n\nSimulate the base model and describe the residual of each observable channel, as measurement minus prediction, for the prompt.\n\nBesides the mean and maximum magnitude, the text reports the temporal trend per channel, comparing the first and last fifth of the samples, and the initial rate of the first dynamic state against the base model. A persistent residual indicates dissipation, a decaying one storage, and the initial rate separates added dissipation from added storage.\n\nArguments\n\nproblem: supplies the base model and the observable states.\ndata_times, data_states: the measurements.\n\nReturns\n\nA multi-line string, or an error message if the simulation fails.\n\n\n\n\n\n","title":"AIRMED._characterise_sensor_residuals"},{"category":"method","location":"internals/#AIRMED._component_law_lines-Tuple{ComponentGuess}","page":"Internals","text":"_component_law_lines(g::ComponentGuess) -> Vector{String}\n\nThe constitutive equations of one library component, as strings for the prompt. The component is instantiated through its own factory, so the prompt shows the equations of the component the framework would build.\n\nEquations referencing a pin are boilerplate, identical for every two-pin component, and are dropped; the prose states them once. State equations are kept, since they are what distinguishes a storage element from a dissipative one.\n\nArguments\n\ng: the component to describe.\n\nReturns\n\nOne string per equation.\n\n\n\n\n\n","title":"AIRMED._component_law_lines"},{"category":"method","location":"internals/#AIRMED._eval_nn_at-Tuple{UpdateResult, AbstractVector}","page":"Internals","text":"_eval_nn_at(update_result, x) -> Vector\n\nEvaluate the trained network at one input vector.\n\nArguments\n\nupdate_result: the trained network, on either the symbolic or the Lux path.\nx: the input vector.\n\nReturns\n\nThe network output.\n\n\n\n\n\n","title":"AIRMED._eval_nn_at"},{"category":"method","location":"internals/#AIRMED._fit_existing_parameters-Tuple{AIRMEDProblem, AbstractVector, AbstractMatrix}","page":"Internals","text":"_fit_existing_parameters(problem, data_times, data_states;\n    max_iters=300, lr=5e-2, holdout_fraction=0.25) -> Union{Nothing, NamedTuple}\n\nRe-fit each problem.adaptable_params entry on its own, since fitting them jointly risks non-identifiability. Same log-space multistart as _fit_proposal_parameters, but on the unmodified model.\n\nEvery parameter is fitted against the full window and against each segment from _plateau_candidates. A clean attempt wins: no residual drift, no parameter at a bound and no grossly mismatched channel. Only if none is clean does the lowest fit_loss decide, which is not comparable across segment lengths.\n\nKeywords\n\nmax_iters, lr, optimization_algorithm: fitting settings.\nholdout_fraction: tail withheld from fitting.\n\nReturns\n\nA NamedTuple (name, param, old_value, new_value, fit_loss, drift_result, at_bound, from_plateau) for the best fit, or nothing if there is none. from_plateau marks a value that explains the settled tail rather than the whole window.\n\n\n\n\n\n","title":"AIRMED._fit_existing_parameters"},{"category":"method","location":"internals/#AIRMED._fit_proposal_parameters-Tuple{Vector{ComponentProposal}, AIRMEDProblem, AbstractVector, AbstractMatrix}","page":"Internals","text":"_fit_proposal_parameters(proposals, problem, data_times, data_states;\n    max_iters, lr, holdout_fraction, optimization_algorithm)\n    -> (updated_proposals, success::Bool, fit_mse::Float64,\n        drift_result::DriftResult, per_sensor::Vector{Pair{String,Float64}},\n        bound_flags::Vector{String})\n\nFit the primary parameter of each proposal, named by ComponentGuess.parameter_name, by minimising the MSE between the adapted trajectory and the measurements.\n\nSteps: build the adapted system once; build one ODEProblem with a setp_oop setter so each evaluation is a remake; run a coordinate-wise log-grid multistart; optimise with box-constrained LBFGS in log-space, falling back to Adam; then re-simulate the full series for the per-sensor RMSE, the drift check and the bound flags that feed the retry loop.\n\nKeywords\n\nmax_iters, lr, optimization_algorithm: optimiser settings.\nholdout_fraction: only the first 1 - holdout_fraction of the series is fitted, while the checks cover all of it.\n\nReturns\n\n(proposals, success, fit_mse, drift_result, per_sensor, bound_flags): the proposals with their fitted values, whether the fit converged, its MSE, the drift check on the adapted model, the RMSE per sensor, and the parameters that ended at a plausibility bound.\n\n\n\n\n\n","title":"AIRMED._fit_proposal_parameters"},{"category":"method","location":"internals/#AIRMED._generate_adaptation_code-Tuple{AIRMEDProblem, Vector{ComponentProposal}}","page":"Internals","text":"_generate_adaptation_code(problem, proposals) -> String\n\nProduce the MTK source that builds the adapted system. The same subsystem in both ports adds two connect lines; two different ones replace the existing connection with two through the new component.\n\nArguments\n\nproblem: supplies the preamble, guesses and base equations.\nproposals: the components to insert.\n\nReturns\n\nThe source as a string.\n\n\n\n\n\n","title":"AIRMED._generate_adaptation_code"},{"category":"method","location":"internals/#AIRMED._hl_accept_factor-Tuple{Int64, Int64}","page":"Internals","text":"_hl_accept_factor(n, w; z = 2.326) -> Float64\n\nFactor by which the residual of a correct law may exceed the noise floor.\n\nSmoothing makes neighbouring samples dependent, leaving about n/w independent samples rather than n, so RMSE/sigma has a sampling spread even when the law is exact. The factor is the 99th percentile of sqrt(chi^2_k / k) under the Wilson-Hilferty approximation; a strict test at 1.0 would reject the correct component in about half of the cases.\n\nArguments\n\nn: number of samples.\nw: smoothing width, so that n/w samples are independent.\n\nKeywords\n\nz: normal quantile behind the bound. Default 2.326, the 99th percentile.\n\nReturns\n\nThe factor, slightly above 1 and approaching it as n/w grows.\n\n\n\n\n\n","title":"AIRMED._hl_accept_factor"},{"category":"method","location":"internals/#AIRMED._hl_basis_predict-Tuple{Any, AbstractVector{<:AbstractString}, AbstractMatrix}","page":"Internals","text":"_hl_basis_predict(fit, X) -> Vector\n\nEvaluate a sparse_regression result on its own basis columns, so the post-fit residual can be compared with the sensor noise floor.\n\nThe surviving terms are reported by name, so each one is looked up in the basis rather than parsed. Parsing would fail on any non-monomial term, such as the sin/cos pair the periodicity check adds.\n\nArguments\n\nfit: one entry returned by sparse_regression.\nnames, Φ: names and columns from the same _build_basis call the fit used, extra_basis included.\n\nReturns\n\nThe predicted response.\n\n\n\n\n\n","title":"AIRMED._hl_basis_predict"},{"category":"method","location":"internals/#AIRMED._hl_dominant_period-Tuple{Any, AbstractVector}","page":"Internals","text":"_hl_dominant_period(t, y; min_cycles = 2, peak_ratio = 8) -> Float64\n\nPeriod of the strongest periodic component of y, from a periodogram of the linearly detrended response. Detrending prevents a slow drift from dominating the low bins.\n\nThe sparse fit uses degree-1 monomials, which approximate a periodic residual by a ramp, so without this an oscillating element looks static. The period comes from the residual itself, never from the true system.\n\nArguments\n\nt, y: time and response series.\n\nKeywords\n\nmin_cycles: lowest admissible number of cycles in the window. Default 2.\npeak_ratio: how far the peak must stand above the median bin. Default 8.\n\nReturns\n\nThe period in the units of t, or NaN if no line stands out.\n\n\n\n\n\n","title":"AIRMED._hl_dominant_period"},{"category":"method","location":"internals/#AIRMED._hl_mode_analysis-Tuple{Any, AbstractVector, Any}","page":"Internals","text":"_hl_mode_analysis(t, y, floor_σ) -> NamedTuple | nothing\n\nSplit the response into two levels, maximising the between-class variance, and report whether it switches rather than varying continuously. A duty-cycled load is described better by its levels, duty ratio and period than by a fitted coefficient.\n\nArguments\n\nt, y: time and response series.\nfloor_σ: the noise floor of the response.\n\nReturns\n\n(lo_lvl, hi_lvl, sep, duty, transitions, period), or nothing if the two levels are not separated well above the noise.\n\n\n\n\n\n","title":"AIRMED._hl_mode_analysis"},{"category":"method","location":"internals/#AIRMED._hl_operating_point!-NTuple{4, Any}","page":"Internals","text":"_hl_operating_point!(io, d, y, floor_σ)\n\nReport the single operating point that remains when nothing in the window varies, with its standard error and the equivalence class of laws through it, since one point cannot identify the form.\n\nArguments\n\nio: buffer to write to.\nd, y: the drive and response series.\nfloor_σ: the noise floor of the response.\n\nReturns\n\nnothing; the report is written to io.\n\n\n\n\n\n","title":"AIRMED._hl_operating_point!"},{"category":"method","location":"internals/#AIRMED._hl_static_test-Tuple{AbstractMatrix, AbstractVector, Any}","page":"Internals","text":"_hl_static_test(X, y, floor_σ; min_gap) -> (frac_explained, memory_rmse)\n\nTest whether the residual is a static function of the measured channels, by comparing the responses of nearest neighbours in input space.\n\nIf two instants look identical to every sensor while the element behaves differently, no static law can describe it: the element has internal state or depends on something unmeasured. No goodness-of-fit value reveals this.\n\nArguments\n\nX: candidate inputs, one column per sample.\ny: the response.\nfloor_σ: the noise floor of the response.\n\nKeywords\n\nmin_gap: neighbours closer than this many samples in time are skipped, since smoothing correlates them.\n\nReturns\n\n(frac_explained, memory_rmse): the share of response variance the inputs explain, and the RMS response difference between sensor-identical instants, in physical units.\n\n\n\n\n\n","title":"AIRMED._hl_static_test"},{"category":"method","location":"internals/#AIRMED._is_connect_eq-Tuple{Any}","page":"Internals","text":"_is_connect_eq(eq) -> Bool\n\ntrue when eq is an explicit connect(...) equation. The algebraic equations implied by a connection give false and are excluded from the generated code and the prompt.\n\n\n\n\n\n","title":"AIRMED._is_connect_eq"},{"category":"method","location":"internals/#AIRMED._mean_drift_explained-Tuple{UpdateResult}","page":"Internals","text":"_mean_drift_explained(result::UpdateResult) -> Union{Float64, Nothing}\n\nMean of the :drift_explained metric across states, skipping NaN entries.\n\nArguments\n\nresult: the training result to read the metric from.\n\nReturns\n\nThe mean fraction of drift explained, or nothing when the metric is unavailable, in which case the caller does not gate on it.\n\n\n\n\n\n","title":"AIRMED._mean_drift_explained"},{"category":"method","location":"internals/#AIRMED._normalized_nn-Tuple{Any, Function, Function, AbstractVector, Any, AbstractVector, AbstractMatrix, Any}","page":"Internals","text":"_normalized_nn(nn, base_ode!, nn_input_fn, u0, tspan, data_times, data_states, p_base)\n    -> Lux.Chain\n\nWrap nn with fixed input z-scoring and output rescaling, so that one set of hyperparameters works across state magnitudes.\n\nArguments\n\nnn: the network to wrap.\nbase_ode!, u0, tspan, p_base: used to simulate the base model, from which the input statistics are taken.\nnn_input_fn: maps state and time to the network inputs.\ndata_times, data_states: supply the output scale, the RMS of the finite-difference derivative.\n\nKeywords\n\nsolver: solver for that simulation. Default Tsit5().\n\nReturns\n\nThe wrapped network. Throws when the simulation fails or the dimensions disagree, after which the caller uses the unwrapped network.\n\n\n\n\n\n","title":"AIRMED._normalized_nn"},{"category":"method","location":"internals/#AIRMED._parse_escalation_json-Tuple{AbstractString}","page":"Internals","text":"_parse_escalation_json(raw_text) -> (explanation::String, recommendation::String, ok::Bool)\n\nParse an escalation explanation into its two fields, reusing _extract_json_block for fenced, bare and truncated JSON.\n\nArguments\n\nraw_text: the response to parse.\n\nReturns\n\n(explanation, recommendation, ok). ok is false on missing JSON, a non-string field or an empty string, and the caller then keeps the raw text.\n\n\n\n\n\n","title":"AIRMED._parse_escalation_json"},{"category":"method","location":"internals/#AIRMED._plateau_candidates-Tuple{Int64}","page":"Internals","text":"_plateau_candidates(n; min_fraction=0.15, max_fraction=0.85,\n                    n_candidates=6, min_samples=8) -> Vector{UnitRange{Int}}\n\nTrailing candidate segments of a series of length n, for windows that contain a transient followed by a settled portion, which no single constant parameter explains across both.\n\nThe boundary is not located by a test on the local derivative, which is unreliable at realistic noise. Several tail lengths are generated instead and the caller keeps the one that fits cleanly.\n\nKeywords\n\nmin_fraction, max_fraction: shortest and longest tail, as a fraction of n. Default 0.15 and 0.85.\nn_candidates: number of segments. Default 6.\nmin_samples: shortest admissible segment. Must exceed drift_config.min_samples by a good margin, since CUSUM spends that many samples on its baseline.\n\nReturns\n\nThe candidate segments as index ranges, longest last, or empty when n is too short.\n\n\n\n\n\n","title":"AIRMED._plateau_candidates"},{"category":"method","location":"internals/#AIRMED._proposals_from_guesses-Tuple{Vector{ComponentHook}, Vector{ComponentGuess}}","page":"Internals","text":"_proposals_from_guesses(hooks, guesses; attempt=1) -> Vector{ComponentProposal}\n\nDemo-mode fallback without an LLM: pair hooks with guesses into concrete proposals.\n\nArguments\n\nhooks: the positions to fill.\nguesses: the candidate component types.\n\nKeywords\n\nattempt: index into an enumeration ordered by increasing structure size, so every one-hook combination is tried before any two-hook one, and a single fault is found early whichever hook it sits at. There are (n_guesses+1)^n_hooks - 1 combinations in total.\n\nReturns\n\nThe proposals of that combination, empty once the enumeration is exhausted.\n\n\n\n\n\n","title":"AIRMED._proposals_from_guesses"},{"category":"method","location":"internals/#AIRMED._score_hooks_by_residuals-Tuple{AIRMEDProblem, AbstractVector, AbstractMatrix}","page":"Internals","text":"_score_hooks_by_residuals(problem, data_times, data_states) -> Vector{Pair{Symbol,Float64}}\n\nwarning: Not used in the prompt: unsound normalisation\nThe score divides by the signal range of each channel, which on a near-constant channel is observation noise, so a constant source can outrank the hook at the fault. Kept for reference; it needs a noise-aware denominator before it can return to the prompt.\n\nScore each hook by the deviation between measurement and base-model prediction at the subsystems it connects.\n\nArguments\n\nproblem: supplies the base model, hooks and observable states.\ndata_times, data_states: the measurements.\n\nReturns\n\n(hook_name => score) pairs, sorted descending, where the score is the maximum relative residual over the connected subsystems as a percentage of the signal range. The maximum rather than the sum, so a subsystem with many channels is not favoured. Empty if the simulation fails or no observables exist.\n\n\n\n\n\n","title":"AIRMED._score_hooks_by_residuals"},{"category":"method","location":"internals/#AIRMED._winnow_proposals-Tuple{Vector{ComponentProposal}, AIRMEDProblem, AbstractVector, AbstractMatrix}","page":"Internals","text":"_winnow_proposals(proposals, problem, data_times, data_states;\n    max_iters, lr, holdout_fraction, optimization_algorithm,\n    max_winnow_size=3, max_winnow_fits=20)\n    -> (updated_proposals, success::Bool, fit_mse::Float64,\n        drift_result::DriftResult, per_sensor::Vector{Pair{String,Float64}},\n        bound_flags::Vector{String})\n\nSelect the simplest adequate subset of the proposals in one response, since _fit_proposal_parameters alone would accept the full set even when a smaller one explains the drift.\n\nProper subsets are tried in increasing size and the smallest one that fits cleanly is accepted, i.e. no residual drift and no parameter at a bound. If none does, the full set is fitted.\n\nKeywords\n\nmax_iters, lr, holdout_fraction, optimization_algorithm: forwarded to the fit.\nmax_winnow_size: largest subset tried. Default 3.\nmax_winnow_fits: cap on the total number of subset fits, after which the full set is used. Default 20.\n\nReturns\n\nAs _fit_proposal_parameters, but for the selected subset.\n\n\n\n\n\n","title":"AIRMED._winnow_proposals"},{"category":"method","location":"internals/#AIRMED._write_llm_log-Tuple{Union{Nothing, AbstractString}, Symbol, AbstractString, AbstractString, AbstractString, AbstractString, Vector{ComponentProposal}}","page":"Internals","text":"_write_llm_log(path, api, model, base_url, prompt, response, proposals)\n\nAppend one call record to path, delimited by a header line so the file stays greppable. Does nothing when path is nothing.\n\nArguments\n\npath: log file, or nothing.\napi, model, base_url: which backend was called.\nprompt, response: the exchange.\nproposals: what was parsed from the response.\n\nReturns\n\nnothing; the record is appended to the file.\n\n\n\n\n\n","title":"AIRMED._write_llm_log"},{"category":"method","location":"internals/#AIRMED.hook_snrs-Tuple{AIRMEDProblem, Any, Any}","page":"Internals","text":"hook_snrs(problem, data_times, data_states; smooth_w) -> Dict{Symbol,Float64}\n\nBalance-law signal-to-noise ratio at every declared hook: sum the response terms with their signs, propagate the sensor variances, and divide by the smoothed noise floor. Public, so a caller can restrict the prompt to the positions the measurements support.\n\nArguments\n\nproblem: supplies hook_residuals and observation_noise.\ndata_times, data_states: the measurements.\n\nKeywords\n\nsmooth_w: width of the moving average. Default 15.\n\nReturns\n\nOne SNR per hook name. A value near 1 means the law closes within noise.\n\n\n\n\n\n","title":"AIRMED.hook_snrs"},{"category":"method","location":"internals/#AIRMED.make_plain_ode_problem-Tuple{Function, Any, Any, Any}","page":"Internals","text":"make_plain_ode_problem(\n    ode!::Function, u0, tspan, p; saveat=nothing\n) -> ODEProblem\n\nBuild an ODEProblem from a plain Julia ODE function.\n\nArguments\n\node!: (du, u, p, t).\nu0, tspan, p: initial state, time span and parameters.\n\nKeywords\n\nsaveat: time points to store.\n\nReturns\n\nThe ODEProblem.\n\n\n\n\n\n","title":"AIRMED.make_plain_ode_problem"}]
}
